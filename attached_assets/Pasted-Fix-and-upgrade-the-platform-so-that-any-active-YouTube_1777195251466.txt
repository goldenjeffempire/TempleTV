Fix and upgrade the platform so that any active YouTube Live stream selected in the admin panel is displayed and played consistently across all primary viewing surfaces, including both the Broadcast Player Page and the Hero Cinematic Section.

Ensure a single unified live stream state is maintained across the entire platform so that when a YouTube Live URL is activated, it automatically propagates in real time to all frontend components without refresh, delay, or desynchronization.

Synchronize the Hero Cinematic Section with the broadcast engine so it mirrors the same active live source as the main player, maintaining identical playback behavior, stream timing, and source switching logic.

Implement a shared live stream controller that:

* Holds the current active YouTube Live source
* Pushes updates instantly to both Broadcast Player and Hero Cinematic Section
* Ensures both components always reflect the same live state
* Prevents conflicts between multiple video instances or outdated sources

Fix playback issues in the Hero Cinematic Section to ensure YouTube Live streams load reliably, autoplay correctly (where supported), and maintain continuous playback without buffering loops, freezing, or desynchronization from the main broadcast player.

Ensure seamless synchronization so that when:

* a new YouTube Live URL is activated in admin
* the stream updates instantly in BOTH sections
* there is no mismatch between hero display and broadcast player

Optimize performance so both sections:

* preload efficiently
* share the same stream source reference
* avoid duplicate network requests or conflicting playback states
* maintain smooth transitions without UI flicker or reload

Add fallback handling so if the YouTube Live stream fails in either section, both surfaces automatically switch together to fallback content (scheduled or backup media) to preserve continuous TV-channel-style experience.

Ensure cross-device consistency so this synchronized behavior works identically across web, mobile, tablet, and smart TV interfaces.

Deliver a fully unified live broadcast experience where the Hero Cinematic Section and Broadcast Player Page always mirror the same real-time YouTube Live stream with perfect synchronization and zero divergence.
