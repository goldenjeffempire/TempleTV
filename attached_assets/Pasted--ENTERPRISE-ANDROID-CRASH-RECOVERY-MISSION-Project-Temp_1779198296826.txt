# ENTERPRISE ANDROID CRASH RECOVERY MISSION

## Project: Temple TV — Production Startup Crash Fix

You are a Senior Android Production Reliability AI Engineer assigned to fully diagnose, repair, stabilize, validate, rebuild, and redeploy the Android application:

Package:
com.templetv.jctm

The application is currently crashing immediately after launch when installed through Google Play Internal Testing.

Android System explicitly reports:
“Temple TV crashed due to its own issues.”

This is a PRODUCTION-BLOCKING SEVERITY-1 INCIDENT.

Your mission is to fix the issue completely like a professional Android engineering team in a real-world enterprise environment.

---

## PRIMARY OBJECTIVE

You must:

1. Identify the exact root cause
2. Fix all startup/runtime crashes
3. Stabilize release builds
4. Ensure Android 10–14 compatibility
5. Rebuild a new production-ready signed .aab
6. Validate the app on real devices/emulators
7. Produce deployment-ready deliverables
8. Ensure the Play Store version launches successfully without crashing

The task is NOT complete until:

* No startup crashes exist
* Release build works
* Play Store install works
* Internal testing works
* Crash-free startup is confirmed

---

## MANDATORY ENGINEERING STANDARDS

You must behave exactly like:

* Senior Android Engineer
* Release Engineer
* DevOps Engineer
* QA Automation Engineer
* Production Reliability Engineer

You must:

* Use enterprise-grade debugging
* Never guess root causes
* Use logs and evidence
* Validate every fix
* Produce professional documentation
* Prevent regressions
* Ensure scalable maintainable fixes

---

## PHASE 1 — FORENSIC INVESTIGATION

STEP 1:
Capture full runtime crash logs.

Run:

adb logcat -c
adb logcat *:E

Launch the app and capture:

* FATAL EXCEPTION
* Caused by
* Stack trace
* Crashing activity
* Line numbers
* Device info
* Android API version

Generate:
ROOT_CAUSE_REPORT.md

The report must include:

* Exact exception type
* Root cause
* Affected module
* Why crash happens
* Why it only affects release builds if applicable

---

## PHASE 2 — RELEASE BUILD ISOLATION

Determine whether:

* Debug build works
* Release build crashes

Build release locally:

./gradlew assembleRelease

Install manually:

adb install app-release.apk

If release-only crash exists:
Investigate:

* R8
* ProGuard
* Obfuscation
* Signing
* Optimizer stripping
* Reflection failures
* Dependency shrinking

---

## PHASE 3 — EMERGENCY STABILIZATION

Temporarily disable optimization:

Inside app/build.gradle:

buildTypes {
release {
minifyEnabled false
shrinkResources false
}
}

Rebuild and retest.

If crash disappears:
Root cause is confirmed as:

* R8/ProGuard/obfuscation

---

## PHASE 4 — PROGUARD/R8 FIXES

Add enterprise keep rules:

-keep class com.templetv.** { *; }

-keep class kotlin.** { *; }
-keep class kotlinx.coroutines.** { *; }

-keep class retrofit2.** { *; }
-keep class okhttp3.** { *; }

-keep class com.google.firebase.** { *; }

-keep class androidx.lifecycle.** { *; }

-keep class com.google.android.exoplayer2.** { *; }

-keepattributes Signature
-keepattributes *Annotation*

Protect:

* reflection
* serialization
* Firebase
* Retrofit
* Gson
* ExoPlayer
* WebView JS interfaces

---

## PHASE 5 — ANDROID 12/13/14 COMPLIANCE

Inspect AndroidManifest.xml.

Ensure launcher activity contains:

android:exported="true"

Verify permissions:

<uses-permission android:name="android.permission.INTERNET"/>

Android 13:

<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

Modern media permissions:

READ_MEDIA_VIDEO
READ_MEDIA_IMAGES
READ_MEDIA_AUDIO

Fix all deprecated Android APIs.

---

## PHASE 6 — STARTUP HARDENING

Audit:

* MainActivity
* SplashActivity
* Application class
* Firebase init
* SharedPreferences
* Intent extras
* Remote config
* Authentication
* API parsing

Replace unsafe code:

BAD:
val user = auth.currentUser!!

GOOD:
val user = auth.currentUser
if (user != null) {
// proceed safely
}

All startup initialization must be:

* null-safe
* lifecycle-safe
* crash-protected

---

## PHASE 7 — FIREBASE VALIDATION

Verify:
google-services.json exists and matches package name.

Validate:

* Firebase initialization
* SHA1 registration
* Play signing fingerprints

Re-download google-services.json if mismatched.

Ensure:

FirebaseApp.initializeApp(this)

works successfully.

---

## PHASE 8 — MEDIA PLAYER & WEBVIEW FIXES

If app uses streaming/video:

Upgrade to latest Media3:

implementation 'androidx.media3:media3-exoplayer:1.3.1'
implementation 'androidx.media3:media3-ui:1.3.1'

Wrap initialization in try/catch.

Protect against:

* codec failures
* WebView crashes
* SSL failures
* invalid URLs

---

## PHASE 9 — NETWORK SECURITY FIX

Create:

res/xml/network_security_config.xml

Add:

<?xml version="1.0" encoding="utf-8"?>

<network-security-config>
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>

Apply in manifest.

Fix:

* SSL issues
* HTTP restrictions
* TLS handshake failures

---

## PHASE 10 — DEPENDENCY & BUILD SYSTEM REPAIR

Run:

./gradlew app:dependencies

Fix:

* duplicate classes
* dependency conflicts
* outdated AndroidX libraries
* Kotlin incompatibilities

Upgrade:

* Gradle
* AGP
* Kotlin
* AndroidX

Recommended:

compileSdk 34
targetSdk 34
minSdk 24

---

## PHASE 11 — CRASHLYTICS DEPLOYMENT

Enable Firebase Crashlytics.

Add:

implementation 'com.google.firebase:firebase-crashlytics'

Upload release mappings.

Ensure future crashes are trackable.

---

## PHASE 12 — CLEAN REBUILD

Execute:

./gradlew clean

Delete:

* /build
* /.gradle
* /app/build

Invalidate IDE caches.

---

## PHASE 13 — GENERATE NEW PRODUCTION BUILD

Increment:

versionCode
versionName

Generate:

./gradlew bundleRelease

Output required:

app-release.aab

Also generate:

* release APK
* mapping.txt

---

## PHASE 14 — QA VALIDATION MATRIX

Test on:

Android 10
Android 11
Android 12
Android 13
Android 14

Devices:

* Samsung
* Xiaomi
* Pixel
* Tecno
* Infinix

Validate:

* cold launch
* login
* video playback
* background restore
* poor network
* airplane mode
* orientation change

---

## PHASE 15 — PLAY STORE DEPLOYMENT

Prepare deployment package:

* .aab
* release notes
* mapping file

Release notes:

Fixed startup crash affecting Android devices.
Improved launch stability and Android 14 compatibility.
Enhanced media player reliability.
Resolved production release runtime issues.

---

## PHASE 16 — FINAL SUCCESS CRITERIA

The mission is ONLY COMPLETE if:

✅ App launches successfully from Play Store
✅ No “App keeps stopping”
✅ No Android System crash popup
✅ Release build stable
✅ Android 10–14 compatible
✅ Crash-free startup validated
✅ New signed .aab generated
✅ Ready for Play Console deployment
✅ Firebase Crashlytics monitoring enabled

---

## FINAL REQUIRED DELIVERABLES

You must produce:

1. ROOT_CAUSE_REPORT.md
2. FIX_IMPLEMENTATION_REPORT.md
3. QA_TEST_REPORT.md
4. Updated source code
5. Signed app-release.aab
6. release APK
7. mapping.txt
8. Deployment instructions
9. Release notes
10. Post-deployment monitoring checklist

Operate exactly like a professional Android production engineering team.
Do not stop until the crash is completely resolved and validated.
