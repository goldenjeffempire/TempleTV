Implement a full professional-grade vMix broadcast integration system that allows the platform to receive, manage, monitor, and broadcast live streams directly from vMix while automatically using a YouTube stream URL as a fallback source whenever the primary live feed becomes unavailable.

Build the integration as a real-world enterprise television broadcast workflow suitable for continuous live channel operations across web, mobile, tablet, and smart TV platforms.

Implement direct vMix live stream ingestion support using professional streaming protocols including RTMP, RTMPS, SRT, and HLS where applicable. Allow broadcast operators to connect vMix outputs directly into the platform using secure ingest endpoints generated by the platform.

Create a dedicated Live Broadcast Control System where administrators can:

* create live channels
* generate secure ingest stream keys
* configure RTMP ingest endpoints
* monitor live stream health in real time
* switch between live and fallback sources
* manage stream status and emergency overrides

Implement real-time stream detection and health monitoring so the platform continuously checks whether the vMix stream is active, healthy, and delivering valid audio/video packets. Detect stream interruptions, freezes, disconnects, bitrate drops, or signal failures automatically.

Implement an intelligent automatic fallback system using YouTube stream URLs as backup broadcast sources. If the vMix stream disconnects, fails, becomes unstable, or stops transmitting, the platform must instantly and automatically switch to the configured YouTube fallback stream without interrupting the broadcast experience or causing dead-air situations.

Ensure fallback switching is seamless with minimal playback interruption and no visible black screens. When the vMix stream becomes stable again, allow intelligent recovery handling that can either:

* automatically switch back to the live vMix stream
* or allow manual operator-controlled switching

Implement a professional broadcast routing layer that prioritizes:

1. Active vMix live feed
2. YouTube fallback stream
3. Emergency fallback media if both fail

Add real-time broadcast telemetry including:

* stream health status
* ingest bitrate monitoring
* dropped frame detection
* connection uptime
* audio/video presence detection
* fallback activation status
* live source currently active

Implement low-latency stream optimization for live broadcasts and ensure compatibility with high-quality production feeds coming from professional vMix broadcasting environments.

Add support for:

* multiple concurrent live channels
* scheduled live broadcasts
* automatic live start detection
* live preview monitoring
* studio confidence monitoring
* stream recording support
* stream redundancy handling

Optimize playback continuity so viewers experience uninterrupted television-style live broadcasting regardless of source switching events.

Implement a professional live broadcast dashboard UI with:

* live source indicators
* active source visualization
* stream health alerts
* fallback activation notifications
* ingest connection diagnostics
* emergency switching controls

Ensure AWS S3 integration continues functioning for archived playback, media assets, thumbnails, and scheduled content while live streaming remains independent and highly resilient.

Implement enterprise-grade security for ingest endpoints and stream keys:

* secure stream authentication
* tokenized stream access
* stream key rotation support
* unauthorized stream prevention

Ensure compatibility with:

* web browsers
* mobile apps
* smart TV applications
* broadcast monitoring systems

Deliver a final architecture that behaves like a professional real-world television broadcast infrastructure where vMix acts as the live production studio source while YouTube automatically maintains channel continuity as a resilient fallback broadcast source whenever live production interruptions occur.
