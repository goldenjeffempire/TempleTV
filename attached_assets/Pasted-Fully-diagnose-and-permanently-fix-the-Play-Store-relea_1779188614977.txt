Fully diagnose and permanently fix the Play Store release crash occurring immediately after launching the Android app from Google Play internal testing.

The AI Agent must:

Identify root cause
Fix all release/runtime issues
Validate on multiple Android versions
Rebuild signed production .aab
Generate release notes
Prepare final upload-ready package
STAGE 1 — ROOT CAUSE INVESTIGATION
1. Collect Crash Logs

Run:

adb logcat | grep AndroidRuntime

and

adb logcat *:E

Capture:

FATAL EXCEPTION
Caused by
Stack trace
Activity name
Line number

Also inspect:

Play Console Android Vitals
Firebase Crashlytics
ANR reports

Mandatory output:

Full root cause report
Exact crashing class/method
Device/API affected
STAGE 2 — RELEASE BUILD VERIFICATION
2. Compare Debug vs Release

Determine whether:

Debug APK works
Release APK crashes

Build release locally:

./gradlew assembleRelease

Install manually:

adb install app-release.apk

If only release crashes:

Investigate R8/ProGuard
Check obfuscation
Check stripped classes
STAGE 3 — FIX COMMON PLAY STORE CRASHES
3. Manifest Validation

Inspect:

AndroidManifest.xml

Ensure:

<application
    android:usesCleartextTraffic="true"
    android:allowBackup="false"
    android:fullBackupContent="false"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">

Verify all Android 12+ exported activities:

android:exported="true"

for launcher activity.

Example:

<activity
    android:name=".MainActivity"
    android:exported="true">
STAGE 4 — SPLASH/MAIN ACTIVITY CRASH FIX
4. Null Safety Initialization

Inspect:

MainActivity
SplashActivity
Application class

Fix:

NullPointerException
Late initialization
Unsafe context access
Uninitialized ViewBinding

Replace unsafe code:

lateinit var player: ExoPlayer

with safe initialization:

private var player: ExoPlayer? = null

and lifecycle-safe cleanup.

STAGE 5 — FIREBASE & GOOGLE SERVICES
5. Verify Firebase Integration

Ensure:

google-services.json exists
SHA1 fingerprints registered
Firebase initialized correctly

Check:

apply plugin: 'com.google.gms.google-services'

Add:

implementation 'com.google.firebase:firebase-analytics'
implementation 'com.google.firebase:firebase-crashlytics'

Initialize:

FirebaseApp.initializeApp(this)
STAGE 6 — R8 / PROGUARD FIXES
6. Disable Obfuscation Temporarily

Test:

minifyEnabled false
shrinkResources false

If crash disappears:
Root cause = obfuscation.

Then add rules:

-keep class com.google.** { *; }
-keep class com.facebook.** { *; }
-keep class retrofit2.** { *; }
-keep class okhttp3.** { *; }
-keep class androidx.lifecycle.** { *; }
-keep class kotlinx.coroutines.** { *; }
-keep class com.templetv.** { *; }

For media/video apps:

-keep class com.google.android.exoplayer2.** { *; }
STAGE 7 — ANDROID 13/14 PERMISSION FIXES
7. Runtime Permission Compliance

Add runtime checks for:

Notifications
Storage/media
Camera
Audio

Example:

if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
    requestPermissions(arrayOf(Manifest.permission.POST_NOTIFICATIONS), 1001)
}

Replace deprecated storage permissions.

Use:

READ_MEDIA_VIDEO
READ_MEDIA_IMAGES
READ_MEDIA_AUDIO
STAGE 8 — NETWORK & SECURITY FIXES
8. Network Security Configuration

Create:

res/xml/network_security_config.xml
<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>

Manifest:

android:networkSecurityConfig="@xml/network_security_config"

Fix SSL/TLS handshake failures.

STAGE 9 — WEBVIEW / VIDEO PLAYER FIX
9. Media & WebView Stabilization

If app streams content:

Update dependencies:

implementation 'androidx.media3:media3-exoplayer:1.3.1'
implementation 'androidx.media3:media3-ui:1.3.1'

Handle player lifecycle properly.

Add crash-safe WebView:

WebView.setWebContentsDebuggingEnabled(true)
STAGE 10 — DEPENDENCY CLEANUP
10. Resolve Dependency Conflicts

Run:

./gradlew app:dependencies

Fix:

Duplicate classes
Version conflicts
Deprecated libraries

Update:

Kotlin
AGP
Gradle
AndroidX

Recommended:

compileSdk 34
targetSdk 34
minSdk 24
STAGE 11 — PLAY STORE SIGNING VALIDATION
11. Verify App Signing

Ensure:

Correct upload keystore
Matching package name
Play App Signing enabled

Validate:

keytool -list -v -keystore upload-keystore.jks
STAGE 12 — CRASHLYTICS INTEGRATION
12. Add Production Crash Monitoring

Enable:

implementation 'com.google.firebase:firebase-crashlytics'

Upload mapping:

./gradlew app:uploadCrashlyticsMappingFileRelease
STAGE 13 — TESTING MATRIX
13. Mandatory Testing

Test on:

Android 10
Android 11
Android 12
Android 13
Android 14

Devices:

Samsung
Xiaomi
Pixel
Tecno/Infinix

Scenarios:

Fresh install
Play Store install
Background resume
Slow internet
Offline mode
Orientation change
STAGE 14 — BUILD CLEANUP
14. Clean & Rebuild

Run:

./gradlew clean

Then:

./gradlew bundleRelease

Generated file:

app/build/outputs/bundle/release/app-release.aab
STAGE 15 — FINAL VALIDATION
15. Pre-Upload Validation

Verify:

App launches successfully
No startup crash
No ANR
No Play Console warnings
No missing native symbols
Release signing valid

Run:

bundletool validate --bundle app-release.aab
STAGE 16 — PLAY STORE DEPLOYMENT
16. Upload New Production Build

Increment:

versionCode
versionName

Example:

versionCode 7
versionName "1.0.7"

Upload:

New .aab
Mapping file
Release notes

Suggested release notes:

Fixed startup crash affecting some Android devices.
Improved app stability and Play Store compatibility.
Enhanced video playback initialization.
Updated Android 14 compatibility.
FINAL DELIVERABLES REQUIRED FROM AI AGENT

The AI Agent must output:

1. Root Cause Report
Exact crash reason
Stack trace
Affected module
2. Fixed Source Code
All corrected files
Updated Gradle configs
Manifest fixes
3. Stability Validation Report
Devices tested
Android versions tested
Pass/fail results
4. Production Build

Generate:

Signed .aab
Release APK
Mapping.txt
5. Deployment Confirmation

Verify:

App opens successfully from Play Store
No startup crashes
Internal testing successful
SUCCESS CRITERIA

The task is complete ONLY IF:

✅ App launches successfully from Google Play
✅ No “App keeps stopping” dialog
✅ Release build stable
✅ Android 10–14 compatible
✅ New .aab generated successfully
✅ Ready for Play Store upload
✅ Crash-free verified through testing