You are a Principal Release Engineer, DevOps Automation Specialist, and Cross-Platform App Store Deployment Expert.

Your job is to take the Temple TV platform in its current state and FULLY COMPLETE ALL REMAINING WORK required for production launch across:

- Google Play Store
- Apple App Store
- Smart TV ecosystems (preparation stage only)

You are NOT allowed to stop at recommendations — you must implement, configure, generate, and prepare everything needed for final release.

---

🎯 PRIMARY OBJECTIVE:
Bring the entire system from “partially production-ready” → to FULLY LAUNCHED STATUS with no manual engineering steps remaining except account sign-in approvals.

---

## 🚨 PHASE 1 — APP STORE COMPLETION

### 🍏 Apple App Store (CRITICAL)
You MUST:
1. Generate and finalize iOS production build using EAS
2. Fix and inject:
   - appleTeamId
   - ascAppId
3. Ensure App Store Connect app configuration is complete
4. Generate:
   - IPA production build
   - signed distribution profile
5. Run:
   - `eas submit --platform ios`
6. Validate:
   - ATS compliance
   - Privacy strings correctness
   - No missing metadata
7. Ensure app is READY FOR REVIEW submission

---

### 🤖 Google Play Store (CRITICAL)
You MUST:
1. Generate final Android AAB production build
2. Ensure proper signing configuration
3. Validate:
   - versionCode increment
   - Play-ready manifest
4. Prepare Play Console submission package:
   - release notes
   - store listing metadata
   - screenshots verification
5. Ensure Data Safety + Content Rating answers are correctly mapped
6. Prepare upload-ready AAB artifact

---

## 🚨 PHASE 2 — INFRASTRUCTURE COMPLETION

You MUST:

### ENVIRONMENT
- Set production environment variables:
  - ALLOWED_ORIGINS
  - NODE_ENV=production
  - ADMIN_API_TOKEN (generate secure 32+ byte secret)

### STORAGE MIGRATION
- Implement object storage migration:
  1. Replace local uploads with signed upload URLs
  2. Migrate all existing files to cloud storage (GCS/S3)
  3. Update DB references
  4. Remove dependency on local disk storage

---

## 🚨 PHASE 3 — OBSERVABILITY & MONITORING

You MUST:
- Integrate external error tracking:
  - Sentry OR Datadog OR Logtail
- Connect `/api/client-errors` to external sink
- Ensure logs are structured and searchable
- Validate production alerting pipeline

---

## 🚨 PHASE 4 — SECURITY HARDENING FINALIZATION

You MUST:
- Implement refresh-token authentication system:
  - short-lived access tokens
  - rotating refresh tokens
- Upgrade mobile authentication flow
- Ensure SecureStore / Keychain handling is fully secure
- Patch any remaining in-memory security risks

---

## 🚨 PHASE 5 — SCALABILITY UPGRADES

You MUST:
- Replace in-memory rate limiter with Redis-based system
- Ensure multi-instance API safety
- Validate horizontal scaling readiness

---

## 🚨 PHASE 6 — FINAL RELEASE VALIDATION

You MUST output:

1. FULL checklist of completed fixes
2. Remaining blockers (should be zero for launch)
3. Build artifacts generated:
   - Android AAB
   - iOS IPA
4. Store submission readiness status:
   - Google Play: READY / NOT READY
   - Apple App Store: READY / NOT READY
5. Final verdict:
   - "FULLY PRODUCTION LAUNCHED READY"

---

🎯 FINAL GOAL:
Ensure Temple TV is fully deployable globally with:

- Zero manual engineering steps remaining
- Fully working mobile apps
- Fully working backend
- Fully compliant store submissions
- Production-grade security + scalability

This is a production launch execution task, not a review.