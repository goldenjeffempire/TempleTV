- [MinIO-only storage — BYTEA removal](minio-only-storage.md) — storage_blobs.data + upload_chunks.fallback_data BYTEA columns dropped; db_fallback path fully removed from chunked-upload.routes + storage-blob-recovery; init-minio-bucket.sh must not use --import instrument.mjs for migration; Replit workflow inline env (VAR=val cmd) only applies to that cmd, not subsequent && cmds.
- [Upload session cleanup hardening](upload-session-cleanup-hardening.md) — 4 gaps: assembling sessions accepted new chunks (add 409 guard); byteOffset overshot sizeBytes (range validation); runSessionCleanup never swept assemblyUploadId _parts rows (add parallel sweep); worker-supervisor maxConsecutiveFailures default was Infinity (fix to 10).
- [Broadcast-first upload pipeline](broadcast-first-upload.md) — never deactivate queue items that have localVideoUrl; CORRUPT_SOURCE+localVideoUrl → warn+MP4 fallback; corruptSourcePattern removed from dispatcher; validator reverse pass allows CORRUPT_SOURCE revival.
- [FastStart non-blocking architecture](faststart-nonblocking.md) — faststart is optimization-only; never a gate; ffmpeg failure path must write FASTSTART_SKIPPED + call enqueueIfMissing + fire bus events before returning; midnight-prayers must NOT gate on faststartApplied; faststart-recovery MUST NOT call isActive=false.
- [Admin accessibility audit](admin-accessibility-audit.md) — all icon-only buttons need aria-label; use 20-line windowed Python scan (not same-line grep); skip shadcn vendored calendar.tsx/sidebar.tsx.
- [Schedule-bridge catch-up window](schedule-bridge-catchup-window.md) — never use exact-minute match; use ±2-min trailing window + firedSlots Map dedup; clear at midnight; mark slot fired BEFORE handleEntry.
- [HLS never-started sweep](hls-never-started-sweep.md) — Gap 2c in validator: transcodingStatus='none'+no hlsMasterUrl+objectPath IS NOT NULL on active queue items never caught by STUCK_HLS_FAILED/STUCK_ENCODING_NO_JOB; every 20 cycles.
- [Faststart-recovery worker hang fix](faststart-recovery-hang-fix.md) — 8 root causes; biggest: dispatchOne awaited runFaststart inline (→ fire-and-forget); sweep guard; withTimeout on all DB/storage; circuit breaker; LIMIT on queries; skip none/queued in probe backfill.
- [Three-source confidence-based bad-URL system](confidence-based-bad-url.md) — requires 2+ independent sources (orchestrator-probe, scanner, storage-recon) to confirm broken URL before bad-URL cache entry; gap1=warn only, gap2=block, gap3=quarantine candidate.
- [Drizzle sql-template array binding bug — ANY() fix](drizzle-any-array-binding.md) — Drizzle expands JS arrays in sql`...` to tuple ($1,$2), not pg array literal; ANY(${arr}::text[]) → ERROR 42846; fix: sql.param(arr) or sql-array-utils helpers.
- [Storage reconciliation false-positive root causes](storage-recon-false-positives.md) — 4 root causes of "All blobs missing" alerts; fixed by: external HLS bypass, YouTube source bypass, size_bytes>0 gate, retry-gated quarantine (default 3 passes).
- [Memory watchdog ops-alert bus fix](memory-watchdog-ops-alert-bus.md) — watchdog routed all alerts via v1 broadcastEngine.emit(); unacked-alerts only listens on adminEventBus → no email ever fired; fix: import adminEventBus statically, replace _emit pattern with adminEventBus.push().
- [s3MirroredAt repair pipeline](s3mirroredat-repair-pipeline.md) — silent .catch(()=>{}) on s3MirroredAt UPDATE permanently orphaned valid videos from scanLibraryAndEnqueue; fix: repairMissingS3MirroredAt() + storage_blobs confirmation + integrated into every scan path.
- [Autonomous storage GC — orphan auto-delete pattern](autonomous-storage-gc.md) — scanOrphanedBlobs() was detect-and-alert-only; converted to auto-delete with ORPHAN_BLOB_AUTO_DELETE + ORPHAN_BLOB_MIN_AGE_HOURS (default 7 days) age gate + newest-blob-at guard per transcoded prefix.
- [HLS pipeline hardening — 5 self-healing patterns](hls-pipeline-hardening.md) — startup sweep, validator reverse-pass, first+last segment probe (ownOrigin 5xx hard-fail), periodic zombie scan, segment count gate at finalization.
- [Graceful-restart reconnect hint pattern](graceful-restart-reconnect-hint.md) — V2ServerFrame "reconnect" type; SSE/WS gateways broadcast hint on SIGTERM; transport handles it by scheduling forceReconnect after retryAfterMs, blocking scheduleReconnect guard.
- [/readyz startup gate via shutdown-flag](readyz-startup-gate.md) — isStartupComplete() in shutdown-flag.ts; markStartupComplete() called at end of main.ts boot; /readyz returns 503 status:"down" until fully booted.
- [Transcoder tick constants adaptive to poll interval](transcoder-tick-constants-adaptive.md) — static TICKS constants calibrated for 10s/tick ran 2× too often at default 5s poll; always use getter computed from env.TRANSCODER_POLL_MS.
- [RN session map key normalization](rn-session-key-normalization.md) — getOrCreateSession must strip trailing slashes from baseUrl before use as map key AND sessions.set(); mismatched key causes silent duplicate WS sessions + battery drain.
- [TV visibility-change reconnect pattern](tv-visibility-reconnect.md) — visibilitychange handler in LiveBroadcastV2.tsx calls forceRebind() when tab was hidden ≥30s; skips 22s dead-socket watchdog; threshold preserves brief tab switches without rebind.
- [Transcoding pipeline hardening — 7 targeted production fixes](transcoding-pipeline-hardening.md) — stage not reset on 4 requeue paths; periodic zombie scan (was startup-only); stale-queued watchdog; on-air MP4 deletion guard; last HLS segment probe; own-origin 5xx fix; startup HLS integrity scan.
- [sourceQuality wire protocol — V2Item + player-core sync](source-quality-wire-protocol.md) — V2Item.sourceQuality added to both domain/types.ts AND lib/player-core/src/types.ts; derived once in reloadInner() (hls/mp4_faststart/mp4_raw); passed through projectItem() both paths; source-upgraded SSE emitted by faststart + transcoder.
- [Schedule-bridge content dispatch pattern](schedule-bridge-dispatch.md) — contentType=video must use enqueueIfMissing(videoId) not scanLibraryAndEnqueue; live/external must emit broadcast-queue-updated; all 3 types must emit broadcast-schedule-updated so admin schedule page refreshes.
- [transcoding-progress SSE emission gap](transcoding-progress-sse-gap.md) — onProgress callback in transcoder.dispatcher.ts updated DB but never called adminEventBus.push("transcoding-progress"); must push every 5s with {videoId,jobId,progress,videoTitle}; admin videos+broadcast-v2 pages both consume this.
- [PG advisory lock in Drizzle transactions](pg-advisory-lock-drizzle.md) — pg_try_advisory_xact_lock returns first row; read as (row as {acquired?:boolean}|undefined)?.acquired; use fixed int32 constant (not hashtext) to avoid IMMUTABLE issues.
- [Broadcast queue admission policy — source-availability-only](broadcast-queue-admission-policy.md) — any video with a URL is admitted; only CORRUPT_SOURCE/SOURCE_MISSING deactivate; faststartApplied=false is warn-only; faststart-recovery targets `failed` status too.
- [Broadcast-v2 connectivity timing fixes](broadcast-v2-connectivity-fixes.md) — naturalEnd retry 30s→5s; FATAL recovery 30s→10s; bad-URL first-failure TTL 90s→20s; anchor fix deferred 15s; preview auto-forceRebind 8s; live-state staleTime 50s→8s.
- [Queue hlsMasterUrl + DB-backed queue backup](queue-hlsmasterurl-and-db-backup.md) — AddQueueItemSchema needs hlsMasterUrl; dispatcher must UPDATE broadcast_queue.hls_master_url at HLS-ready; queue backup is DB-primary (/tmp secondary).
- [LiveHero broadcast-v2 gating pattern](livehero-broadcast-gating.md) — showLiveBroadcast must be !isLive (not hasBroadcast/broadcastItem!==null); Home.tsx onSelect __live__ needs else-clause for override mode; metadata panel must use !isLive not hasBroadcast.
- [finalizeFromDbFallback multipart abort](finalize-fallback-abort.md) — assemblyUploadId must be declared outside the try block; catch must call abortMultipartUpload on failure or orphaned _parts rows accumulate; stale-session cleanup uses sessions.uploadId (original ingest), NOT the reassembly uploadId created inside finalizeFromDbFallback.
- [HLS concurrency memory budget](hls-concurrency-memory-budget.md) — HLS_MAX_CONCURRENT must stay ≤10 on Replit; 20 concurrent = 480 MiB from HLS alone; MemoryCache MAX_SIZE 10k→1k + 60s sweep.
- [Upload lifecycle audit — 3 pipeline fixes](upload-lifecycle-audit-fixes.md) — resetFaststartOrphans missing enqueueTranscode call; fillQueueFromLibrary queued pre-committed blobs; pre-commit transcodingStatus 'queued'→'none'.
- [forceRebind vs forceReconnect — player overlay buttons](forcereconn-vs-forcerebind.md) — overlay "Try Again"/"Tap to reconnect" must call forceRebind (machine.requestManualRebind + transport.forceReconnect), NOT forceReconnect alone; both web and RN hooks must expose forceRebind.
- [Upload pipeline recovery gap — faststart + HLS](upload-recovery-faststart-hls.md) — onReady hook called enqueueIfMissing for crash-recovered sessions but never ran faststart or enqueueTranscode; recovered videos skipped midnight-prayers + never got HLS.
- [Broadcast-first upload gaps — retry+recovery ffprobe](broadcast-first-upload-gaps.md) — spawnAssemblyRetry and boot-recovery paths lacked ffprobe+container-gate before enqueueIfMissing; broadcast_queue.duration_secs update was premature (0 rows updated — queue row didn't exist yet); fix: hoist ffprobeDurSecs, update AFTER enqueue.
- [HLS auth gaps](hls-auth-gaps.md) — probeDurationFromUrl needs withHlsToken; .replit.app missing from SSRF allowlist; mediaIntegrityScanner must be in index.ts export statement.
- [HLS probe localhost bypass — orchestrator + scanner 401 fix](hls-probe-localhost-bypass.md) — probeUrlReachability/scanner probe external API_ORIGIN URL → 401 when REQUIRE_HLS_TOKEN=true; fix: toLocalhostProbeUrl() rewrites own-origin /api/hls/ → 127.0.0.1:PORT; INTERNAL_HLS_BYPASS_SECRET adds X-Internal-Token header belt-and-suspenders; isInternalRequest() replaces isLoopbackIp() in video-serve.routes.ts; POST /broadcast-v2/revalidate-sources = full recovery endpoint.
- [ffprobe duration N/A — 3-strategy fix](ffprobe-duration-strategy.md) — format=duration returns N/A for fMP4/TS/fragmented containers; fix: query format+stream in one JSON call (S1); deep-scan 100M (S2); ffmpeg -hide_banner -i stderr "Duration:" parse (S3). reprobe endpoint must join videosTable to get objectPath and call probeUploadedDuration first.
- [Dead-air SSRF localhost fix](dead-air-ssrf-localhost-fix.md) — isAllowed() must NOT block localhost in production without API_ORIGIN/RENDER_EXTERNAL_URL; adds REPLIT_DEV_DOMAIN fallback + hash-reset on all-rejected.
- [Mobile "Couldn't refresh" root causes](mobile-couldnt-refresh-bugs.md) — 5 bugs: 304 never cleared isStale/refreshFailed; no bg retry after failure (stuck 5min); no network-recovery trigger; hero suppressEventsOverride not wired; WS heartbeat watchdog fired every ~60s on healthy connection (lastHeartbeatMsRef never updated from actual WS frames).
- [Ops-alert cooldown pattern](ops-alert-cooldown-pattern.md) — polling workers that emit ops-alert on every cycle need lastOpsAlertAtMs+cooldown; reset on recovery so first breach always fires.
- [HLS/transcoding audit — 8 production fixes](hls-transcoding-audit-fixes.md) — H264_LEVEL_HEX missing "2.1" (wrong CODECS for 240p); yadif deinterlace for interlaced broadcast; audio probe retry; #EXT-X-INDEPENDENT-SEGMENTS in master; X-Accel-Buffering:no on 206 Range.
- [API version fallback + version bump pipeline](api-version-pipeline.md) — npm_package_version unset with bare `node` invocation; all 3 fallback strings must be updated manually; version-bump.sh + release-all.sh + production-release.yml must all bump api-server/package.json in lockstep.
- [Dual-prefix boot-timer double-fire](dual-prefix-boot-timer.md) — registerDomainRoutes registered at /api/v1 AND /api means any Fastify plugin with a boot-time setTimeout fires twice; guard with a module-level boolean flag.
- [Checkpoint timeout false-positive pattern](checkpoint-timeout-false-positive.md) — Promise.race with setTimeout: always clearTimeout() in writePromise.finally() or the timeout callback fires after write succeeds → spurious WARN every cycle.
- [Runtime index IMMUTABLE + missing-column failures](runtime-index-immutable-missing.md) — text→timestamptz cast is STABLE not IMMUTABLE; is_active/series_id may not exist; use imported_at alone for sort indexes; wrap optional columns in DO $$ IF EXISTS block.
- [Replit DATABASE_URL vs PG* env vars — two different DBs](replit-db-url-vs-pgvars.md) — DATABASE_URL points to Neon; PG* vars point to Replit heliumdb; API pool uses PG*, psql $DATABASE_URL hits Neon; always validate indexes/columns via PG* vars.
- [DO-EXECUTE nested dollar-quoting fails silently in pg client](pg-nested-dollar-quoting.md) — DO $$ BEGIN ... EXECUTE $idx$ CREATE INDEX ... $idx$; END $$ causes silent no-op via pg lib; use run() with bare CREATE INDEX IF NOT EXISTS instead.
- [objectPath absolute-URL data quality bug](objectpath-absolute-url-bug.md) — some managed_videos rows store absolute URL as objectPath → faststart CORRUPT_UPLOAD false-positive; fixed: startup SQL repair + runFaststart normalises on-the-fly + backfillPlaceholderDurations skips full URLs.
- [Production hardening + performance sprint](production-hardening-perf-sprint.md) — 9 indexes added to ensureRuntimeIndexes; HLS CDN variant-playlist rewrite fix (isMaster guard removed); orchestrator no-op reload hash skip; TV service worker; admin lazy-image audit.
- [In-process cache().get<T>() type must match Zod schema](cache-get-typed-return.md) — cache().get<unknown>() causes TS2769 in typed Fastify handlers; use z.infer<typeof ResponseSchema> or exact shape.
- [Broadcast sync telemetry sprint](broadcast-sync-telemetry.md) — DriftAggregator (ring-1024); POST /report-position (drift recording); GET /sync-reference (authoritative position); transport 30s drift reporter; admin ViewerSyncCard (P50/P95/max).
- [report-position and checkpoint auth bug](report-position-auth-bug.md) — both routes had userGuard but transport sends no auth header → 401 always; fix: remove userGuard from both; /state got 2s in-process cache invalidated on frame events; TransportConfig.getAuthToken added.
- [Mobile cursor pagination sentinel fix](mobile-cursor-pagination-sentinel.md) — API returns total=-1/totalPages=-1 for sort=newest/oldest (cursor mode); mobile must handle -1 sentinel and use nextCursor for hasMore/loadMore; PlayerSnapshot has no viewerCount (use useBroadcastSync); apiBase defined per-component scope.
- [Performance audit sprint](performance-audit-sprint.md) — stampede-safe Cache.getOrSet(); HTTP keepAlive 75s/headersTimeout 80s; 240p HLS tier; EMA fast-track α=0.5 for 2–5s drift; compress threshold 512B; ETag+304+X-Accel-Buffering on HLS; 2 new runtime perf indexes.
- [HLS segment in-process LRU cache](hls-segment-cache.md) — HlsSegmentLru bypasses DB for repeated .ts fetches; HLS_SEGMENT_CACHE_MB=64; A2 path buffers stream for cache population; X-Cache: HIT/MISS header.
- [Production hardening sprint — Tasks 1-6](production-hardening-sprint.md) — DB FK hardening, player-core FATAL jitter+bindInactive guard, health monitor `Math.max(GRACE, duration*1.5)`, restoredCycleAnchor empty-queue preservation, TV VRAM cap, iOS heartbeat watchdog, didJustFinish positionMillis>0 guard.
- [Broadcast-v2 hardening sprint](broadcast-v2-hardening-sprint.md) — 8 fixes: worker circuit-open SSE+email alert, health monitor overrun detection, tighter STALE/RECOVERY defaults (3/7 min), naturalItemEnd durationWriteInFlight dedup, API_ORIGIN production warn, audio probe timeout ops-alert.
- [Production fixes — unhandled rejections + push OOM](production-fixes-unhandled-rejection.md) — poll() outer try/catch; cleanup.service void .catch(); push-delivery paginated (500/page keyset) + return bug fixed.
- [Platform audit sprint 115 — false positive patterns](sprint-115-false-positive-patterns.md) — 4 real fixes applied; 15+ confirmed false positives across admin SPA, broadcast-v2, mobile; key patterns documented.
- [SSE write() backpressure fix](sse-backpressure-fix.md) — cleanupRef forward-ref + writeStallCount: close connection after 3 consecutive write()→false returns (~30s zombie slow client); wire cleanupRef=cleanup after cleanup is defined.
- [SSE zombie detection — all endpoints](sse-zombie-all-endpoints.md) — pattern: lastXxxWriteOkMs + zombieCheck interval (30s check, 90s idle timeout) on every SSE endpoint; broadcast-v2 uses writeStallCount≥3 instead; midnight-prayers uses reply.raw.destroy() to unblock the await-Promise pattern.
- [Mobile SecureStore transient retry](mobile-securestore-retry.md) — Android keystore unavailable post-reboot; retry Promise.all reads once with 500ms delay; propagate on 2nd failure → outer catch → stay logged out.
- [Mobile NetworkContext dynamic API probe](mobile-network-context-probe.md) — import getApiBase(); build probe list at module init: [apiBase+/api/healthz, 1.1.1.1, ubuntu] — app's own URL first so VPN/dev environments report correctly.
- [Fastify schema 429 responses](fastify-schema-429.md) — routes calling reply.code(429).send({error}) must declare 429: z.object({error:z.string()}) or tsc fails TS2345/TS2353.
- [Admin panel query invalidation matrix](admin-query-invalidation.md) — mutation → query key matrix; catalog cache key prefix is catalog2 (not catalog); selectedIds must clear on page change.
- [Admin broadcast-v2 SSE invalidation gap](admin-sse-invalidation-gap.md) — broadcast-queue-updated SSE handler must invalidate ALL queue-derived keys including diagnostics + queue-sync-status; one-click destructive actions need AlertDialog confirm.
- [Broadcast queue duplicate-video guard](broadcast-queue-duplicate-guard.md) — 4-layer guard: addToQueue pre-check in TX; enqueueIfMissing catches ConflictError + 23505; admin route returns 409; validator detects+auto-fixes DUPLICATE_ACTIVE_VIDEO.
- [Memory leak comprehensive audit](memory-leak-audit.md) — full audit of all in-memory Maps/Sets; one real leak: routeAggregates in slow-request-capture.ts (GC interval fix); all others confirmed bounded.
- [Mobile HLS playback end-guard and drift-seek fixes](mobile-hls-playback.md) — 4-layer fix for "single segment replay" loop: HLS_END_GUARD_MS=8000 > threshold=5000; live HLS retry uses playAsync(); drift guard skips < 30 s re-seeks.
- [Android "Tuning in" overlay stuck — same-URL recovery rebind bug](android-overlay-stuck.md) — expo-av won't re-fire onLoad when source string unchanged; RECOVERING_PRIMARY rebind of same URL left FSM stuck; fix: lastLoadedUrlRef + fast-path + onReadyForDisplay.
- [HLS.js buffer tuning for 24/7 broadcast TV](hls-buffer-tuning.md) — 30 s forward / 0 s back on TV surfaces; 30/30 on admin preview; 60 s maxMax. Prevents long-session VRAM exhaustion on Tizen/webOS.
- [Upload queue persistence + auth-expiry gaps](upload-queue-persistence-gaps.md) — enqueue() missing wasUserPaused/wasRestored; _onAuthExpired and autoResumeInterrupted never implemented; autoResumeInterrupted must be called from AuthenticatedApp useEffect.
- [PiP entered while override already active](pip-entered-during-override.md) — V2PlayerContainer YouTube-override-in-PiP exit must use COMBINED (override && in-PiP) condition + reactive isInPip prop; one-shot ref gated to primary driver.
- [V2Player videoReady poster-lift](v2player-videoready-poster-lift.md) — mobile poster gate must lift via isPlaying fallback in onPlaybackStatusUpdate (not only onReadyForDisplay); never gate all visual fallbacks on posterUrl.
- [sequenceStale false-positive on long items](sequence-stale-false-positive.md) — SEQUENCE_STALE_THRESHOLD_MS=5min caused ok:false for items >5min; withinPlaybackWindow guard skips stale flag while elapsed < durationMs + 2-min grace.
- [ensureRuntimeIndexes silent-skip bug](ensure-runtime-indexes-silent-skip.md) — single try/catch + fire-and-forget caused all 10 runtime indexes absent from DB; fixed with per-index run() helper + await in main.ts.
- [RN/web natural-end guard parity](rn-natural-end-isStopped-parity.md) — react.ts and react-native.ts natural-end retry chains must BOTH carry 2× transport.isStopped guards or evicted RN sessions POST /natural-end forever.
- [Async-setTimeout crash pattern](async-settimeout-crash.md) — setTimeout(async () => { await db... }) without try/catch throws unhandledRejection crashing Node ≥15; fix: void (async()=>{try{...}catch{logger.warn}})() + t.unref().
- [Player-core + health audit bugs sprint](player-core-health-audit.md) — naturalEnd retry setTimeout leaks (transport.isStopped guard); EMA no large-jump reset (re-seed at >5000ms); health stuck only sequence=0 (added sequenceStale).
- [HLS.js & transcoder playback quality sprint 22+23](hls-playback-quality-sprint22.md) — abrEwmaDefaultEstimate 8M→10M; +abrEwmaFastLive/SlowLive; +liveSyncDurationCount; transcoder: deblock=-1:-1 added (comma delimiter for ffmpeg 7.x).
- [SIGTERM hardening sprint 68](sigterm-hardening-sprint68.md) — SHUTDOWN_PRECLOSE_DELAY_MS=10000 in render.yaml; API_ORIGIN=https://api.templetv.org.ng required; unhandledRejection/uncaughtException → graceful shutdown; 25s force-exit safety net.
- [Mobile auto-update system — rate-limit bugs](mobile-update-ratelimit-bugs.md) — clearVersionCheckTimestamp() must be called before runVersionCheck() on push-tap and forced checks; markVersionCheckDone() before check blocks it; AppState active must call both runOTACheck AND runVersionCheck.
- [Broadcast resilience hardening — transport + machine + orchestrator](broadcast-resilience-hardening.md) — snapshot cache 4h; offline grace 24h; session TTL 2h; fatalAttemptCount/fatalEnteredAtMs in PlayerSnapshot; queue backup file (/tmp); SELF_HEAL_EMPTY_MS 10s→5s.
- [Transport offline-skip reconnect gap](transport-offline-reconnect.md) — scheduleReconnect() skips when offline; online handler MUST call forceReconnect() when no socket+no timer or device never auto-reconnects; snapshot TTL 4h + 24h offline grace.
- [Video technical metadata pipeline](video-technical-metadata.md) — probeVideoMetadata() in transcoder.service.ts: single ffprobe pass after upload assembly saves videoCodec/audioCodec/videoBitrate/videoWidth/videoHeight to DB; non-fatal (returns null on failure).
- [Subagent rate-limit syntax patterns](subagent-ratelimit-syntax-patterns.md) — 8 distinct syntax error variants subagents introduce when adding rateLimit+429 schemas; always global-grep-and-fix before first rebuild.
- [VIDEO_COLS raw SQL column reference pattern](video-cols-safe-fallback.md) — any raw sql`…${videos.colName}…` in VIDEO_COLS causes 42703 on pre-migration DBs; add SAFE_CATALOG_COLS + try/catch isUndefinedColumnError fallback.
- [moov tail-scan false-positive CORRUPT_SOURCE](moov-tail-scan-false-positive.md) — 64 KiB tail scan missed large moov atoms (1–5 MiB for 30-min H.264); fix: ffprobe full-file probe; retryJob+retryAllFailed unblocked for CORRUPT_SOURCE when objectPath IS NOT NULL.
- [YouTube Live Poller quota burn](youtube-live-poller-quota-burn.md) — YtLivePoller search.list (100 units/call × every 90s) bypassed isQuotaExhausted(); fix: check quota in poll() and set cooldown to midnight UTC.
- [ffprobe source validation sprint 86](sprint-86-ffprobe-validation.md) — validateLocalSourceFile (stat+size+magic-bytes) + probeCanDecodeFirstFrame (ffmpeg mdat decode test) added; 3-stage probeUploadedContainerValidity.
- [CORS preflight DELETE failure — explicit allowedHeaders fix](cors-explicit-allowedheaders.md) — explicit allowedHeaders+methods+maxAge on @fastify/cors; X-Chunk-Index+X-Chunk-Checksum missing → "Network error during chunk upload" on cross-origin XHR.
- [isProdNodeEnv TDZ crash pattern](isprodNodeEnv-tdz.md) — pre-flight block ran BEFORE isProdNodeEnv was declared; fix: use env.NODE_ENV === "production" directly in pre-flight; never reference a let/const declared later in the same function.
- [Stale onrender.com URL rewrite in queue.repo](stale-onrender-url-rewrite.md) — normalizeQueueUrl() passed *.onrender.com URLs unchanged → dead host in orchestrator; fix: rewrite hostname to API_ORIGIN/RENDER_EXTERNAL_URL.
- [SOURCE_MISSING terminal transcode code](source-missing-terminal-code.md) — missing source blob must be classified terminal like CORRUPT_SOURCE across all 6 special-case sites or it churns retry budgets; only LOCAL misses are Terminal.
- [FATAL-state + missing-HLS root cause fix](fatal-state-missing-hls-fix.md) — autoEnqueueMissingHls never suppressed raw MP4 URLs while transcoding queued → RECOVERING→SKIP_PENDING(×3)→FATAL; fix: markBadUrlWithTtl after each enqueueTranscode.
- [x264-params deblock delimiter + orphaned ffmpeg-promise crash](x264-params-deblock-delimiter.md) — `-x264-params` uses `:` as delimiter so `deblock=-1:-1` breaks libx264 (exit 234) under ffmpeg 7.x → use comma; orphaned ffmpeg promise needs .catch() or FATAL crash loop.
- [pnpm security override strategy](dependency-override-strategy.md) — version-pin overrides for multi-major pkgs (ws 6/7/8); audit may flag outside advisory range; csrf/ws/sql.raw SAST = confirmed false positives.
- [Media-proxy fetch-stream teardown](media-proxy-stream-teardown.md) — proxying fetch ReadableStream must abort upstream on client disconnect; HLS segment aborts leak sockets + log "stream closed prematurely" without it.
- [Orchestrator probe HEAD→GET fallback](orchestrator-probe-head-fallback.md) — probeUrlReachability must GET-fallback on HEAD 4xx; cancel GET body to avoid streaming full file on Range-ignoring origins.
- [Clock calibration on every snapshot ingress](clock-calibration-all-snapshot-paths.md) — REST /state snapshots must calibrate clockOffset like WS/SSE frames; cache fallback must NOT calibrate (stale serverTimeMs).
- [Global NODE_ENV=production breaks dev workflows](mobile-blank-clear-flag.md) — workspace-global NODE_ENV=production poisons dev workflows; force NODE_ENV=development in each non-production workflow.
- [Single-item queue loop black-screen fix](single-item-loop-blackscreen.md) — lastEndedItemStartsAtMs field on PlayerMachine; guard clears when server advances startsAtMs for same item ID (new cycle), not after 30 s TTL.
- [WS concurrent-resume phantom-listener fix](ws-concurrent-resume-phantom.md) — two rapid resume messages cause double onFrame registration; guard: `if (activeFrameHandler !== bufferFrame) return` after DB await.
- [registerDomainRoutes prefix doubling](register-domain-routes-prefix.md) — routes inside registerDomainRoutes already inherit /api; adding { prefix: "/api" } doubles it to /api/api/…. adminOpsRoutes has { prefix: "/admin" } so route URLs must be relative.
- [Hero+Player live override sync](live-override-sync.md) — 3 bugs: YouTube override → expo-av recovery spiral; HLS/RTMP override poster stuck forever (LIVE_OVERRIDE_ACTIVE missing from isPlayingFamily); hero thumb resizeMode.
- [V2PlayerContainer fresh-mount watchdog hazard](v2player-fresh-mount-watchdog.md) — load-timeout + buffering-stall watchdogs arm against fresh Video on Player open or fullscreen toggle; fsmIsWaiting gate required.
- [Mobile Hero+Player FSM isolation & PiP hardening](mobile-hero-player-pip-hardening.md) — v1-WS drove Hero broadcast state causing V2 desync; V2PlayerContainer conditional mount caused HLS reload on reconnect; midnight-prayers per-instance.
- [useV2BroadcastNative multi-consumer session dedup](v2-native-session-dedup.md) — stall reporter + escape valve must live in getOrCreateSession not hook body — 3 simultaneous callers tripled POST /report-stall and armed 3 escape timers.
- [Broadcast-v2 shutdown memory leaks](broadcast-v2-shutdown-leaks.md) — orchestrator.stop() never called (7 timers); workerSupervisor.stopAll() never called (5 workers); v2 SSE invisible to sseCounter drain loop.
- [Upload-library-queue sync hardening](upload-library-queue-sync.md) — onComplete missing broadcast-queue invalidation; enqueueIfMissing event only fires on enqueued=true; object_path unique partial index required.
- [Memory watchdog two-tier threshold](memory-watchdog-two-tier.md) — MEMORY_WARN_RSS_MB warns only; MEMORY_RESTART_RSS_MB controls restart; env.ts defaults were inverted (WARN=1500 > RESTART=600 caused restart-before-warn).
- [HLS OOM memory math](hls-oom-memory-math.md) — pg BYTEA hex strings add 16 MiB V8 heap per concurrent HLS request; HLS_MAX_CONCURRENT=30 on 460 MiB heap → OOM; production MEMORY thresholds were inverted.
- [SSE KNOWN_EVENTS completeness](sse-known-events-completeness.md) — useSSEEvent() silently never fires if event name missing from KNOWN_EVENTS in sse-context.tsx; always add both there AND in summarize().
- [Orchestrator probe must bypass media-proxy](orchestrator-probe-bypass-media-proxy.md) — probes must call extractRawProbeUrl() to strip media-proxy wrapper; redirect:"manual" SSRF guard + prod upload redirect → false 403.
- [Zero-downtime shutdown via shutdown-flag singleton](zero-downtime-shutdown.md) — /healthz returns 503 on SIGTERM via shutdown-flag.ts; SHUTDOWN_PRECLOSE_DELAY_MS delays close for LB drain.
- [Storage stream shutdown drain](storage-stream-shutdown-drain.md) — streamChunked generators race DB pool.end() at shutdown; drain uses Math.max(env.SHUTDOWN_DRAIN_MS, 5_000); --expose-gc required for proactive gc().
- [YouTube sync hardening](youtube-sync-hardening.md) — drizzle-kit push silently skips columns; recoverStaleSyncLogs() for orphaned "running" entries; isTransientPgError SQLSTATE-based.
- [Enterprise broadcast reliability overhaul](enterprise-broadcast-reliability.md) — transcodingErrorCode column; retryJob/retryAllFailed block CORRUPT_SOURCE; auto-enqueue excludes failed+!faststart; validator UNPLAYABLE_CORRUPT_UPLOAD check+auto-fix+reverse-pass.
- [Transcoder videopath normalization](transcoder-videopath-normalization.md) — localVideoUrl (/api/v1/uploads/…) ≠ objectPath (uploads/…); only bare key works with storage().getObject(); normaliseVideoPath() in enqueueTranscode.
- [Env-var emergency filler/failover REMOVED](broadcast-v2-filler-hardening.md) — EMERGENCY_FILLER_URL + BROADCAST_FAILOVER_HLS_URL and filler-insertion/failover-sentinel logic deleted; queue exhaustion goes off-air. Do not re-add.
- [Upload RSS memory hardening sprint](upload-rss-memory-hardening.md) — semaphore (MAX_CONCURRENT_CHUNK_DB_OPS=6); body buffer null-out after uploadPart; MEMORY_RESTART_RS_MB default 490→600; DB_POOL_MAX 10→20.
- [Broadcast-v2 reprobe endpoint & Vite proxy completeness](broadcast-v2-reprobe-and-proxy.md) — managed_videos.duration is text (String(secs)), not integer; broadcast-v2 SSE/WS/queue proxy rules must precede generic /api in vite.config.ts.
- [Android 15 edge-to-edge + PiP](android15-edge-to-edge-pip.md) — edge-to-edge plugin modifies AppTheme styles; local Expo module (expo-pip-android) provides PiP; hook uses relative import not package name.
- [Broadcast queue duplicate-video guard](broadcast-queue-duplicate-guard.md) — 4-layer guard: addToQueue pre-check in TX; enqueueIfMissing catches ConflictError + 23505; admin route returns 409; validator detects+auto-fixes DUPLICATE_ACTIVE_VIDEO.
- [Mobile cache stale-closure pattern](mobile-cache-stale-closure.md) — useCallback([]) hooks can't read state; use ref.current instead; AuthContext USER_SCOPED_STORAGE_PREFIXES must include every new cache key prefix.
- [CORRUPT_SOURCE early-gate distinction pattern](corrupt-source-early-gate-pattern.md) — probeUploadedContainerValidity returns unrecoverable?/kind?; gates only hard-fail on unrecoverable===true; Strategy 4 fMP4 in remuxForFaststart.
- [Production audit — SMTP alerting gaps](production-audit-smtp-alerting.md) — sendAdminAlert never wired to any monitor; SMTP pre-flight absent; transport singleton never resets on EAUTH; no admin test-email endpoint.
- [Autonomous broadcast platform — round 2](autonomous-broadcast-round2.md) — HLS variant deep probe, 3 maintenance indexes, ffmpeg zombie scan, storage health monitor, queue health guard, all wired into /health.
- [Upload assembly recovery on server restart](upload-assembly-restart-recovery.md) — onReady hook blindly deleted video rows for assembling sessions on boot; fix: probe storage blob size first — keep row if size matches sizeBytes.
- [Queue validator false-positive UNPLAYABLE_CORRUPT_UPLOAD](queue-validator-faststart-null.md) — `!row.vFaststart` catches NULL (never attempted) AND false (explicitly failed); must be `=== false`. Same bug in auto-fix filter.
- [WS frameQueue cap + overridePlaying reset](ws-framecap-overrideplaying.md) — WS resume handler needs frameQueue cap matching SSE (500); TV LiveBroadcastV2 overridePlaying must reset when kind changes HLS→YouTube.
- [RN janitor hookCount — sessions never evict](rn-janitor-hookcount.md) — stallListener+escapeValveListener added to snapshotListeners keep size≥2 forever; janitor must track hookCount separately.
- [YouTube queue item fix + WS gateway phantom listener + web adapter boundKind](youtube-queue-and-ws-gateway-bugs.md) — YouTube bind must fire buffer-ready immediately; close handler must use activeFrameHandler + set socketClosed=true.
- [onBufferStalled YouTube-awareness fix](buffer-stalled-youtube-fix.md) — LIVE_OVERRIDE_ACTIVE must gate escalation on override.kind: YouTube overrides use iframe so stall is harmless; HLS/RTMP must escalate.
- [Broadcast sync — MP4 and periodic drift correction](broadcast-sync-mp4-drift.md) — resolvePositionSecs must cover hls/mp4/dash (not just hls); PLAYING state must emit play on every snapshot (not only driftMs>5s); replayStateToAdapter must cover mp4/dash for SPA remounts.
- [Upload pipeline — disappearing video fixes](upload-pipeline-disappearing-video-fixes.md) — 3 bugs: cancelJob→'none' causes auto-enqueue loop; uploadTelemetry.success() reached outer catch; assemblyCommitted flag guards blob deletion in Path A catch.
- [Broadcast reconnect & domain migration](broadcast-reconnect-domain-migration.md) — api.→admin.templetv.org.ng migration checklist; FATAL from SKIP_PENDING cycles (threshold=3, 30s auto-recovery); escape-valve stall re-report.
- [Prod-sync media proxy bypass in dev](prod-sync-media-proxy-bypass.md) — API_ORIGIN=prod in dev caused prod-sync MP4s to skip proxying → CORP-blocked; fix: IS_PROD_NODE_ENV guard on API_ORIGIN in queue.repo.ts.
- [HLS probe localhost bypass — orchestrator + scanner 401 fix](hls-probe-localhost-bypass.md) — probeUrlReachability/scanner probe external API_ORIGIN URL → 401 when REQUIRE_HLS_TOKEN=true; fix: toLocalhostProbeUrl() rewrites own-origin /api/hls/ → 127.0.0.1:PORT.
