- [Expo SDK ladder upgrade gotchas (54→57)](expo-sdk-upgrade-gotchas.md) — catalog-hardcoding, TS6 baseUrl, absoluteFillObject removal, app.json schema changes, native-module EventEmitter modernization.
- [lucide-react stale pnpm peer-hash path](lucide-react-stale-pnpm-hash.md) — hardcoded `node_modules/.pnpm/pkg@v_react@v/...` workaround path breaks on every React version bump; looks identical to store corruption.
- [Phantom Channels navigation root causes and permanent fixes](nav-phantom-channels-fix.md) — `segments` in Supervisor effect deps + wrong initialRouteName + cold-start auto-nav were all 4 root causes; fixed with segmentsRef pattern.
- [expo-modules-core 3.x Coroutine DSL](expo-modules-core-coroutine-dsl.md) — AsyncFunction crossinline lambdas cannot call suspend; use `AsyncFunction("name") Coroutine { ... }` for suspend bodies in EMC 3.x.
- [Stuck-assembly scanner and session-completed retry](stuck-assembly-scanner.md) — 3 gaps causing "Assembling…" stuck state; fixed with 30s retry + finalize-status s3MirroredAt check + periodic 5-min scanner.
- [Natural-end retry timing — broadcast pipeline](natural-end-retry-timing.md) — naturalEndRetryDelays [300,800,2000]ms; machine 2s TTL guard + 5 retries + 1s inner poll; STALL_REBIND_MS 20s; mobile vendor must be kept in sync with lib/player-core/src/.
- [Player-core vendor sync — mobile vendor divergence rules](player-core-vendor-sync.md) — mobile uses artifacts/mobile/vendor/player-core/ (not lib/); every lib edit MUST mirror to vendor; V2SourceQuality + naturalEndRetryDelays both fixed July 2026.
- [BYTEA MP4 false-positive stall fixes](bytea-mp4-false-positive-stalls.md) — MAX_NOTIFY_ACTIVE_STREAK 40→120, BIND_LOAD_TIMEOUT_MS 20s→40s, markBadUrlWithTtl for /api/v1/uploads/ in /report-stall.
- [Player primary-retry budget before server-side skip](player-retry-budget.md) — MAX_PRIMARY_RETRIES 2→3 in machine.ts; admitted MP4 bind failures are overwhelmingly transient, so more retries beat skips.
- [Faststart schema removal — Drizzle query gaps](faststart-schema-removal-gaps.md) — videosTable.faststartApplied is undefined in Drizzle schema; use sql`false`/`NULL::boolean` literals in select/set sites; .set({faststartApplied}) writes must be removed.
- [Upload-to-broadcast pipeline — immediate enqueue design](upload-broadcast-immediate-enqueue.md) — validationStatus is advisory only (never gates broadcast admission); s3MirroredAt stamped directly in finalize INSERT; VALIDATION_FAILED in queue-validator is warn not auto-deactivate; reverse pass re-activates old validation_failed items.
- [Upload-broadcast audit fixes](upload-broadcast-audit-fixes.md) — 4 bugs fixed: validationStatus gate in isPlayableForBroadcast; ne() NULL trap (4 sites); validation not wired into finalize; validator missing VALIDATION_FAILED eviction.
- [Auto-refill no-op cooldown pattern](auto-refill-noop-cooldown.md) — _lastRefillAtMs only updates on successful refill; YouTube-only deploys never trigger it so cooldown never engages → 2 DB queries/45s forever. Fix: separate _lastNoOpAtMs / NO_OP_COOLDOWN_MS reset on no-candidate runs.
- [Terminal error code gate for broadcast admission](terminal-error-broadcast-gate.md) — isPlayableForBroadcast + repairMissingS3MirroredAt must both skip ASSEMBLY_FAILED/CORRUPT_SOURCE/SOURCE_MISSING; NOT IN on nullable col is a SQL NULL trap — use or(isNull, not(inArray)) pattern.
- [Upload reconciler lookback window](upload-reconciler-lookback.md) — uploadQueueReconciler had 24h window; widened to 7 days to catch uploads that slipped through enqueue >1 day ago (scanLibraryAndEnqueue only covers this when queue is empty).
- [Dead stub worker detection pattern](dead-stub-worker-detection.md) — a worker can be fully wired (scheduler+admin trigger+dashboard) yet its body is a permanent no-op stub from a prior pipeline migration; verify by tracing real DB ops, not just clean boot logs.
- [SOURCE_MISSING terminal transcode error code](source-missing-terminal-code.md) — permanent missing-blob failures need a full parallel set of classification edits (dispatcher/retry/validator/UI); a UI-flag gap (sourceAvailable) caused a futile retry-loop button in production.
- [Orphaned-blob ops-alert false positives](orphaned-blob-alerts.md) — upload-integrity-monitor must only raise error ops-alert when corrupt blob has a referencing managed_videos row; orphaned staging keys (keys_parts/…, keys_meta/…) with no video ref are routine GC — skip the alert.
- [Auto-heal QUEUE_EMPTY suppression for YouTube-only](autoheal-queue-empty-yt-only.md) — auto-heal-monitor emits QUEUE_EMPTY error+noop every 60s on YouTube-only deployments; suppress alert+scan when ytShuffleFallback.isActive (same pattern as queue-health-guard).
- [Error handling hierarchy and AppError classification](error-handling-hierarchy.md) — BadGateway(502)/GatewayTimeout(504)/UnprocessableEntity(422) added; classifyRawError() in error-handler auto-promotes ECONNREFUSED/AbortError to 502/504; ConflictError for "already in progress" guards. Upstream API failures (EAS/GitHub/YouTube) must use BadGatewayError not plain Error. Object.assign(new Error(),{statusCode}) also works for one-off errors.
- [CORRUPT_SOURCE post-assembly over-classification](corrupt-source-post-assembly.md) — assemblyCommitted=true catch block must NOT mark transcodingStatus=failed+CORRUPT_SOURCE; reactivateSystemDeactivated() permanently excludes CORRUPT_SOURCE rows. Fixed: split catch by assemblyCommitted; post-step throws leave video at "none" for reconciler auto-recovery ≤60s.
- [Upload 500s under concurrent load — all DB queries must be guarded](upload-db-guard.md) — pg pool exhaustion + BYTEA assembly make bare db.select/update in chunk+finalize handlers throw unhandled → 500; wrap every pre-semaphore DB op in try/catch → 503; video INSERT must be 503 not 500; error-handler classifyRawError must cover all pg pool/connection patterns.
- [broadcastEngine.reload() must never throw from routes](broadcast-engine-reload-safe.md) — broadcastEngine.reload() (v1 engine) can throw after successful DB writes; routes only caught ConflictError leaving BadRequestError/reload errors as 500. Fix: reloadV1Engine() try/catch helper in broadcast.service.ts; BadRequestError→422 + 23505→409 catches in broadcast.routes.ts POST /queue; 404 on DELETE /queue/:id. v2 reload always fires via adminEventBus bus bridge regardless.
- [Broadcast startup library scan](startup-queue-scan.md) — post-boot repairMissingS3MirroredAt + scanLibraryAndEnqueue("startup") in broadcast-v2/index.ts catches orphaned queue rows the 24-h reconciler misses (DB restore, videos >24 h old, silently-failed s3MirroredAt stamps).
- [Route response schema completeness](route-schema-completeness.md) — every status code explicitly returned via reply.code(N).send() MUST be in the route's response schema; missing entry causes unschema'd fast-json-stringify (benign) but also means monitoring/OpenAPI misses it. transcode-remote had 202/400/404/409/507 missing — added. InternalError preferred over plain Error for assembly invariant violations (proper Sentry capture).
- [Upload integrity monitor — BYTEA scan elimination](upload-integrity-monitor-bytea.md) — octet_length(data)/SUM(octet_length) in WHERE causes full BYTEA reads; use size_bytes col + SET LOCAL statement_timeout tx for bounded checks; COUNT(*) for orphan-parts. Note: SET LOCAL statement_timeout must use sql.raw() not sql template — pg driver rejects parameterized SET statements.
- [MOOV_PLACEMENT validation must be warn not fail](moov-placement-warn.md) — checkMoovPlacement(faststartApplied=false) was returning "fail" → validationStatus="failed" → isPlayableForBroadcast() blocked video from broadcast. MP4-only pipeline: raw MP4 is broadcast-eligible; faststart is background optimization only. Use "warn".
- [Recovery service MP4-pipeline classification](recovery-service-mp4.md) — deep-recovery classifies "none"+localVideoUrl+s3MirroredAt as missing_from_queue (→ enqueueIfMissing); NOT never_processed (→ enqueueTranscode). Add local_video_url+s3_mirrored_at to snapshot query. never_processed only for legacy objectPath-but-no-localVideoUrl rows.
- [Integrity monitor sequential passes](integrity-monitor-sequential.md) — scanMissingBlobs + scanOrphanedParts must run sequentially; parallel execution causes orphan pass to delete parts for sessions scanMissingBlobs just reset from 'completed'→'assembling' for recovery.
- [Faststart as broadcast admission gate](faststart-admission-gate.md) — finalize bg now runs runFaststart BEFORE enqueueIfMissing; s3MirroredAt gates isPlayableForBroadcast (undefined=skip gate, null=block); auto-refill SQL requires s3_mirrored_at IS NOT NULL for local videos + repairMissingS3MirroredAt pre-pass; TRIGGER_MS 30→60 min, INTERVAL_MS 90→45 s.
- [clearLoadTimeout must be stable useCallback](clear-load-timeout-stable.md) — clearLoadTimeout must be useCallback([]) not a plain function; plain function can't safely appear in handleError/handleVideoError deps; ghost 8s timer fires second buffer-error against recovery bind.
- [Video delete — synchronous queue cleanup](video-delete-sync-cleanup.md) — broadcast_queue/transcoding_jobs/series_episodes/playlist_videos/upload_sessions must be synchronous before 200; broadcast-queue-updated must fire unconditionally (not only if items found).
- [Blob-not-found permanent fix](blob-not-found-fix.md) — 4-layer fix: spawnAssemblyRetry blob-exists shortcut + storageBackend="db" + MISSING_BLOB queue validator + startup blob audit.
- [Blob-not-found root cause and permanent fix](blob-not-found-root-cause.md) — sweepOrphanedSessions deleted blobs when completedVideoId=NULL; fixed with object_path cross-check + STORAGE_LOST error code + UI distinction.
- [Memory leak fixes (RSS/External/ArrayBuffers)](memory-leak-fixes.md) — unacked-alerts store unbounded growth; storage dec() double-decrement; readChunks missing _shuttingDown guard; watchdog missing GC on ArrayBuffers alert.
- [Faststart recovery Category C + timing fixes](faststart-recovery-category-c.md) — recovery worker now catches transcodingStatus='none' videos (inline trigger missed); initial delay 5→1 min; batch 10→20; faststartApplied=false hoisted to top-level AND.
- [Admin broadcastFilter is client-side only](admin-broadcast-filter-client-side.md) — broadcastFilter in videos.tsx is applied client-side via displayVideos; API has no broadcastReady param; do NOT add it to query key or send to API.
- [Play In-App Updates architecture](play-in-app-updates.md) — CompletableDeferred bridges OnActivityResult→AsyncFunction; runCheckRef breaks circular useCallback deps; scheduleRetry reads ref not closure.
- [EAS Replit git index workaround](eas-replit-git-index-workaround.md) — cp .git/index /tmp/eas-git-index + GIT_INDEX_FILE=/tmp/eas-git-index lets EAS archive the full 22 MB workspace (with pnpm-lock.yaml) without triggering Replit's index.lock sandbox restriction.
- [schedule day_of_week=313 bug](schedule-dow-bug.md) — nowMinutes() (0–1439) confused with todayDow() (0–6); 5×60+13=313 at 05:13; fixed with guard, bounds-check, DB CHECK constraint, and assertValidDayOfWeek helper.
- [zod esbuild externalize on Replit](zod-esbuild-externalize.md) — zod v4 ESM files fail esbuild bundling through pnpm virtual store; must be in external[] in build.mjs.
- [Faststart-recovery probe deadman hang](faststart-probe-deadman.md) — slow-path probe loop must have outer stage budget + row limit sized so (limit × per-item timeout) < outer budget < worker deadman.
- [YouTube-only broadcast architecture](youtube-only-broadcast.md) — 100% YouTube catalog (960 videos); broadcast_queue always empty; ytShuffleFallback is the permanent broadcast driver, not a fallback. Duration stored as text seconds string.
- [queue-self-healing worker missing registration](queue-self-healing-missing.md) — queueSelfHealingWorker.scan() existed but was never spawned via workerSupervisor.spawn(); caused health score < 100 and nextRevalidationAt=null; fixed in broadcast-v2/index.ts startSupervisedWorkers().
- [ytShuffleFallback pre-advance timer](yt-shuffle-pre-advance.md) — scheduleOverrideEndTimer fires PRE_ADVANCE_LEAD_MS=2000ms early so advance() DB write completes before old override expires; guard uses Math.abs(overrideEndsAtMs-endsAtMs) > PRE_ADVANCE_LEAD_MS+3000 to detect replaced overrides.
- [YouTube non-embeddable dead-air fix](yt-embeddable-dead-air-fix.md) — 3-layer fix: DB is_embeddable filter + YouTube sync status part + client window.message→POST /yt-playback-error self-healing.
- [yt-shuffle orchestrator-level throttle](yt-shuffle-orchestrator-throttle.md) — rate-limit activate() at the orchestrator call site (lastYtShuffleActivateAttemptMs), not inside activate(); internal cooldown fails due to fire-and-forget async patterns.
- [Memory watchdog slope window — startup false positive](memory-watchdog-slope-window.md) — all slope-growth funcs must return null when window < MIN_SLOPE_WINDOW_MS=120s; V8 JIT in first 30s triggers 75 MB/min false-positive alert.
- [Production hardening deep-review findings](production-hardening-deep-review.md) — Storage/HLS/queue/watchdog all already production-grade; 2 genuine gaps fixed: HLS oversized-segment streaming bypass + orphan-cleanup deactivation reason code.
- [Render Disk / persistent storage paths](render-storage-paths.md) — STORAGE_PATH env var routes scratch/backup dirs to /var/data; media is in PostgreSQL (never disappears); only FFmpeg scratch + broadcast state/queue backup JSON benefit from the persistent mount.
- [normalizeQueueUrl REPLIT_DEV_DOMAIN gap](normalize-queue-url-replit-dev.md) — REPLIT_DEV_DOMAIN must come BEFORE API_ORIGIN in normalizeQueueUrl()+getOwnBase(); on Replit with NODE_ENV=production, API_ORIGIN absolutizes BYTEA upload URLs to a different server.
- [broadcast_queue hlsMasterUrl column removed](broadcast-queue-hls-column-removed.md) — hls_master_url dropped from broadcast_queue (still on videos/channel_queue); a lingering key in Drizzle .values()/.set() THROWS at runtime (unknown column) = latent 500, not type drift. Grep all write sites after any column removal.
- [MP4-only pipeline — HLS removal](mp4-only-pipeline.md) — Platform switched to raw MP4 (no HLS transcoding). TRANSCODER_DISABLE=1 + TRANSCODING_AUTO_RETRY_DISABLE=1 in Start API workflow. Source resolver sort order changed to mp4:0, hls:1. Storage reconciliation worker Pass B (library HLS integrity) removed; recovery service stages 1+2 (HLS master/segment recovery) removed; enqueueTranscode replaced with enqueueIfMissing in recovery stage 4.
- [PostgreSQL BYTEA storage — full S3/MinIO replacement](pg-bytea-storage.md) — storage.ts replaced with PostgresObjectStorage; storage_blobs.data BYTEA + storage_upload_parts table added; @aws-sdk removed; lockfile edited directly (pnpm install OOMs on lockfile update — edit pnpm-lock.yaml specifier section instead).
- [Drizzle sql-template array binding bug — ANY() fix](drizzle-any-array-binding.md) — Drizzle expands JS arrays in sql`...` to tuple ($1,$2), not pg array literal; ANY(${arr}::text[]) → ERROR 42846; fix: sql.param(arr) or sql-array-utils helpers.
- [Faststart memory gate](faststart-memory-gate.md) — gate must use min(MEMORY_WARN_RSS_MB, MEMORY_RESTART_RSS_MB−100); plain MEMORY_WARN_RSS_MB leaves only 40 MB of headroom on 512 MiB hosts, still enough for ffmpeg spawn to cross the restart threshold.
- [All-blocked dead-air + restart cascade](all-blocked-dead-air-cascade.md) — boot revalidation `items.length>0` early-return ignores all-bad-URL-cached state; allBlockedSinceMs TTL (was 90s) must be < watchdog restart window (~80s); both fixed together break the infinite loop.
- [HLS concurrency memory budget](hls-concurrency-memory-budget.md) — HLS_MAX_CONCURRENT must stay ≤10 on Replit; 20 concurrent = 480 MiB from HLS alone; MemoryCache MAX_SIZE 10k→1k + 60s sweep.
- [HLS OOM memory math](hls-oom-memory-math.md) — pg BYTEA hex strings add 16 MiB V8 heap per concurrent HLS request; HLS_MAX_CONCURRENT=30 on 460 MiB heap → OOM; production MEMORY thresholds were inverted.
- [HLS probe localhost bypass — orchestrator + scanner 401 fix](hls-probe-localhost-bypass.md) — probeUrlReachability/scanner probe external API_ORIGIN URL → 401 when REQUIRE_HLS_TOKEN=true; fix: toLocalhostProbeUrl() rewrites own-origin /api/hls/ → 127.0.0.1:PORT; INTERNAL_HLS_BYPASS_SECRET adds X-Internal-Token header belt-and-suspenders; isInternalRequest() replaces isLoopbackIp() in video-serve.routes.ts; POST /broadcast-v2/revalidate-sources = full recovery endpoint.
- [Scanner MP4 probe loopback gap](scanner-mp4-probe-loopback.md) — scanner's toLocalhostProbeUrl matched only /hls/ not /uploads/; probe for mp4 kind used raw external URL not localhostUrl → Replit proxy timeouts falsely marked BYTEA uploads as unreachable. Fix: extend regex to hls|uploads? AND apply toLocalhostProbeUrl for mp4 too.
- [DISABLED_HEARTBEAT schema mismatch](disabled-heartbeat-schema.md) — transcoder.dispatcher.ts DISABLED_HEARTBEAT was missing lastCompletedAt/Id/Status + storageCircuitOpenUntil/ErrorStreak; route schema required all 5 → ResponseSerializationError → 500 on /admin/transcoder-status.
- [queueStats stub return shape mismatch](queue-stats-stub-shape.md) — disabled transcoder.queue.ts queueStats() returned {pending,encoding,done,...} but every consumer + TranscodingQueueStatsSchema expects {activeCount,queuedCount,completedToday,failedToday} → 500 on /admin/transcoding/queue.
- [RN session map key normalization](rn-session-key-normalization.md) — getOrCreateSession must strip trailing slashes from baseUrl before use as map key AND sessions.set(); mismatched key causes silent duplicate WS sessions + battery drain.
- [PG advisory lock in Drizzle transactions](pg-advisory-lock-drizzle.md) — pg_try_advisory_xact_lock returns first row; read as (row as {acquired?:boolean}|undefined)?.acquired; use fixed int32 constant (not hashtext) to avoid IMMUTABLE issues.
- [Dual-prefix boot-timer double-fire](dual-prefix-boot-timer.md) — registerDomainRoutes registered at /api/v1 AND /api means any Fastify plugin with a boot-time setTimeout fires twice; guard with a module-level boolean flag.
- [Replit DATABASE_URL vs PG* env vars — two different DBs](replit-db-url-vs-pgvars.md) — DATABASE_URL points to Neon; PG* vars point to Replit heliumdb; API pool uses PG*, psql $DATABASE_URL hits Neon; always validate indexes/columns via PG* vars.
- [DO-EXECUTE nested dollar-quoting fails silently in pg client](pg-nested-dollar-quoting.md) — DO $$ BEGIN ... EXECUTE $idx$ CREATE INDEX ... $idx$; END $$ causes silent no-op via pg lib; use run() with bare CREATE INDEX IF NOT EXISTS instead.
- [HLS.js buffer tuning for 24/7 broadcast TV](hls-buffer-tuning.md) — 45 s forward / 0 s back on TV surfaces (unconstrained); 20 s for constrained (pre-2019 webOS/Tizen≤3); 60 s maxMax unconstrained. Admin preview: 30 s forward / 30 s back (operator review window), 60 s maxMax.
- [Broadcast preload lead alignment](broadcast-preload-lead.md) — NEAR_END_LEAD_SECS (web.ts) and PRELOAD_LEAD_MS (machine.ts) must both equal server BROADCAST_PRELOAD_LEAD_MS (default 120 s); mismatched values (was 90 s) create a race where client fires near-end trigger before server emits preload frame.
- [Async-setTimeout crash pattern](async-settimeout-crash.md) — setTimeout(async () => { await db... }) without try/catch throws unhandledRejection crashing Node ≥15; fix: void (async()=>{try{...}catch{logger.warn}})() + t.unref().
- [Subagent rate-limit syntax patterns](subagent-ratelimit-syntax-patterns.md) — 8 distinct syntax error variants subagents introduce when adding rateLimit+429 schemas; always global-grep-and-fix before first rebuild.
- [Route schema vs ProblemDetails error field](429-schema-problem-details-mismatch.md) — route schemas declare `{ error: z.string() }` for 4xx AND 5xx; error-handler must always include `error: string` (use generic "Internal server error" for 500, err.message for 4xx). Missing `error` on ANY declared status → ResponseSerializationError → Fastify fallback "Internal Server Error" with no diagnostics.
- [getBadUrlStats field name — blockedCount not size](getBadUrlStats-field-name.md) — getBadUrlStats() returns { blockedCount, entries } NOT { size }; accessing .size returns undefined → z.number() Zod serialization failure on any route that sends it in a response body.
- [VIDEO_COLS raw SQL column reference pattern](video-cols-safe-fallback.md) — any raw sql`…${videos.colName}…` in VIDEO_COLS causes 42703 on pre-migration DBs; add SAFE_CATALOG_COLS + try/catch isUndefinedColumnError fallback.
- [isProdNodeEnv TDZ crash pattern](isprodNodeEnv-tdz.md) — pre-flight block ran BEFORE isProdNodeEnv was declared; fix: use env.NODE_ENV === "production" directly in pre-flight; never reference a let/const declared later in the same function.
- [pnpm security override strategy](dependency-override-strategy.md) — version-pin overrides for multi-major pkgs (ws 6/7/8); audit may flag outside advisory range; csrf/ws/sql.raw SAST = confirmed false positives.
- [Media-proxy fetch-stream teardown](media-proxy-stream-teardown.md) — proxying fetch ReadableStream must abort upstream on client disconnect; HLS segment aborts leak sockets + log "stream closed prematurely" without it.
- [Watchdog alert–action coupling](watchdog-alert-action-coupling.md) — every new alert flag must be wired into GC trigger, cache purge, Sentry capture, AND periodic re-action; arrayBuffersAlertActive was missing from GC + had no re-trim.
- [Global NODE_ENV=production breaks dev workflows](mobile-blank-clear-flag.md) — workspace-global NODE_ENV=production poisons dev workflows; force NODE_ENV=development in each non-production workflow.
- [registerDomainRoutes prefix doubling](register-domain-routes-prefix.md) — routes inside registerDomainRoutes already inherit /api; adding { prefix: "/api" } doubles it to /api/api/…. adminOpsRoutes has { prefix: "/admin" } so route URLs must be relative.
- [Memory watchdog two-tier threshold](memory-watchdog-two-tier.md) — MEMORY_WARN_RSS_MB warns only; MEMORY_RESTART_RSS_MB controls restart; env.ts defaults were inverted (WARN=1500 > RESTART=600 caused restart-before-warn).
- [SSE KNOWN_EVENTS completeness](sse-known-events-completeness.md) — useSSEEvent() silently never fires if event name missing from KNOWN_EVENTS in sse-context.tsx; always add both there AND in summarize().
- [Admin SSE rate-limit death spiral](admin-sse-rate-limit.md) — per-minute rateLimit on long-lived SSE causes "Reconnecting" death spiral on server restart; use concurrent-per-IP Map instead (same pattern as realtime/sse.gateway.ts).
- [Transcoder videopath normalization](transcoder-videopath-normalization.md) — localVideoUrl (/api/v1/uploads/…) ≠ objectPath (uploads/…); only bare key works with storage().getObject(); normaliseVideoPath() in enqueueTranscode.
- [Env-var emergency filler/failover REMOVED](broadcast-v2-filler-hardening.md) — EMERGENCY_FILLER_URL + BROADCAST_FAILOVER_HLS_URL and filler-insertion/failover-sentinel logic deleted; queue exhaustion goes off-air. Do not re-add.
- [Android 15 edge-to-edge + PiP](android15-edge-to-edge-pip.md) — edge-to-edge plugin modifies AppTheme styles; local Expo module (expo-pip-android) provides PiP; hook uses relative import not package name.
- [Mobile cache stale-closure pattern](mobile-cache-stale-closure.md) — useCallback([]) hooks can't read state; use ref.current instead; AuthContext USER_SCOPED_STORAGE_PREFIXES must include every new cache key prefix.
- [Queue validator false-positive UNPLAYABLE_CORRUPT_UPLOAD](queue-validator-faststart-null.md) — `!row.vFaststart` catches NULL (never attempted) AND false (explicitly failed); must be `=== false`. Same bug in auto-fix filter.
- [EAS build OOM root causes](eas-build-oom.md) — 5 causes: NODE_OPTIONS 4096 (raise to 8192), Metro maxWorkers unset (cap at 2), shaka-player in mobile deps (remove), SENTRY_DISABLE missing on staging, resourceClass medium for prod (use large). NOTE: large requires paid EAS plan; templedev is free-tier → use medium + METRO_MAX_WORKERS=1 instead.
- [EAS frozen-lockfile failure — workspace.yaml overrides mismatch](eas-workspace-overrides-lockfile-mismatch.md) — pnpm-workspace.yaml overrides section out of sync with pnpm-lock.yaml → instant UNKNOWN_ERROR in Install phase (~16s build). Fix: keep all overrides in package.json only.
- [Reanimated v4 requires react-native-worklets peer](reanimated-v4-worklets.md) — reanimated@4.x extracted worklets into a separate package; its Babel plugin does require('react-native-worklets/plugin') at bundle time → MODULE_NOT_FOUND if not installed; fix: add react-native-worklets@~0.8.3 as direct dep in package.json + full lockfile entry.
- [EAS build Node+pnpm version pins](eas-node-pnpm-version-pins.md) — eas.json "node" and "pnpm" fields must use full semver (e.g. "24.1.0"/"10.26.1"), not majors; EAS worker default is pnpm 8.7.5+Node 22 which can't read pnpm-10 lockfiles.
- [Expo local module Android Gradle — 4 build fixes](expo-local-module-android-build.md) — do NOT use `apply plugin: 'expo-module'` in a local module; use standard Android library; AsyncFunction void lambdas need explicit `return@AsyncFunction null` and trailing `null`; compileSdk must be ≥36 for androidx.core:core:1.17+.
- [PanResponder stale closure via useRef.create](panresponder-stale-closure.md) — PanResponder.create() wrapped in useRef captures all outer variables at initial render; props and computed values (e.g. sheetHeight) that change over lifetime must be read through a ref updated every render.
- [YouTube-only broadcast alert suppression](youtube-only-broadcast-alerting.md) — queue-exhaustion/auto-refill/health-guard all fire false-positive alerts on YouTube-only deployments; fix: orchestrator.getOverrideState() + 90s boot delay + ytShuffleFallback.isActive checks.
- [YouTube Live Poller quota burn](youtube-live-poller-quota-burn.md) — YtLivePoller search.list (100 units/call × every 90s) bypassed isQuotaExhausted(); fix: check quota in poll() and set cooldown to midnight UTC.
- [Broadcast 1800s placeholder duration — MP4 pipeline gaps](broadcast-duration-placeholder.md) — backfillPlaceholderDurations skipped 'none' transcodingStatus; health monitor delay was 45s; orchestrator never triggered immediate backfill on reload; upload-queue never persisted finalizeOnly on finalize entry.
- [Production restart-loop root causes + fixes](production-restart-loop-fixes.md) — MEMORY_RESTART_RSS_MB=2500 on 2 GiB host, HLS_MAX_CONCURRENT=20, MEMORY_ABSOLUTE_MAX_RSS_MB disabled were root causes; fixed thresholds + 15s self-healing relief pass before SIGTERM; process split for dev isolation.
- [YouTube-grade chat upgrade — key design constraints](chat-upgrade-design.md) — reactions are in-memory only (ephemeral); batch frame (100ms window) coalesces WS writes; `chat_settings` table + `role`/`isHighlighted` on `chat_messages`; 4 type mirrors must stay in lock-step.
- [Circuit-breaker + source approval design](circuit-breaker-source-approval.md) — tuned thresholds and in-memory approval gate for broadcast pipeline probe system.
- [Zero-byte blob storage integrity hardening](zero-byte-blob-hardening.md) — putObject rejects empty bodies; completeMultipartUpload post-assembly guard; Phase 3 of scanOrphanedBlobs deletes immediately + triggers waterfall; boot-repair hook for specific video IDs.
- [Asset self-healing state machine](asset-self-healing-design.md) — queue_asset_health table + assetHealthRepo + queueSelfHealingWorker + REST endpoints + admin AssetHealthPanel; clearSourceApproval(itemId) takes 1 arg not 2.
- [asset-health repairLog raw SQL snake_case](asset-health-repair-log-sql.md) — Drizzle .set() raw sql`` for repair_log must use snake_case "repair_log" NOT camelCase "repairLog"; 3 occurrences in clearExpiredBlocked, bulkReset, bulkApprove cause 42703 column-not-exist errors.
- [Premature auto-advance — seek-past-end false ended](premature-auto-advance.md) — 4-layer fix: (1) hasSeenTimeupdateSinceLoad flag in web adapter suppresses ended before any timeupdate; (2) FSM onBufferEnded state guard (PLAYING/PREPARING_NEXT only); (3) 2s min-playback guard in onBufferEnded; (4) server naturalItemEnd rejects elapsed < max(15s, 5% durationSecs).
- [1800s placeholder — naturalItemEnd + validator 4-fix](placeholder-duration-fixes.md) — naturalItemEnd: when DB lookup also returns 1800, use 15s min not 90s; validator: add hlsMasterUrl to hasAnyUrl+ORPHANED_VIDEO_REF; SUSPICIOUS_DURATION reprobe rewritten to localhost URL; new PLACEHOLDER_DURATION auto-fix copies real duration from managed_videos to broadcast_queue.
- [Midnight prayers server-side window enforcement](midnight-prayers-window.md) — getSnapshot() was missing time-window check entirely; itemWatchTimer pushed transitions 24/7; client isInMpWindow() used device local time not station timezone. Fix: pure window-utils.ts + server enforcement in getSnapshot() + client Intl.DateTimeFormat. Tests: node:test in __tests__/window.test.ts, run with tsx/esm.
- [Midnight-prayers category leakage — 4-layer fix](midnight-prayers-category-leakage.md) — midnight-prayers videos could leak into main broadcast queue, YouTube shuffle, and schedule-bridge; main channel REST /state never gated during the window. Fix: ne(category,"midnight-prayers") in auto-enqueue+yt-shuffle queries; schedule-bridge imports midnightPrayersService+isWindowActive and gates video/playlist entries in handleEntry(); GET /state returns synthetic offline_hold when mpWindowOpen && mpHasVideos.
- [YouTube dual-slot transition timing](yt-dual-slot-timing.md) — optimal A/B iframe swap delays in LiveBroadcastV2.tsx; gapless unmute is immediate, non-gapless unmute 300ms, preload handoffs 100/300ms; nextYtVideoId already typed on V2Snapshot (no cast needed).
- [YouTube override mobile dead-air — load-timeout cascade](yt-override-mobile-dead-air.md) — BroadcastBuffer load timeout arms even when url=null; onSnapshot calls engageOverride on every keepalive. Two fixes: url!==null guard + onSnapshot same-ID guard.
- [FastStart pipeline fully removed](faststart-pipeline-removed.md) — faststart & scheduleVideoValidation removed from automated upload flow (finalize bg, assembly-retry, orchestrator, REST routes, supervised workers); validationStatus gate removed from isPlayableForBroadcast; raw MP4 enqueued immediately after s3MirroredAt blob commit.
- [Video validation service — e2e pipeline](video-validation-service.md) — 9-check ffprobe/ffmpeg validation; status null/pending/running/passed/warn = allow; failed = blocked; all isPlayableForBroadcast call sites must select validationStatus.
- [Production probe URL normalization bugs](probe-url-normalization-bugs.md) — 5 probe bugs fixed: missing import, raw-URL cache key mismatches, loopback bypass gaps, and sourceHash≠URL confusion in approve endpoint.
- [Upload transient auto-resume](upload-transient-auto-resume.md) — multi-GB admin uploads must park+auto-resume (same sessionId) on transient errors (drop/5xx/499/stall/timeout), not fail; user Pause must handle BOTH timer & networkPausedIds parking.
- [Modern Android auto-PiP (setAutoEnterEnabled)](modern-auto-pip-android.md) — auto-enter is API31+ via setPictureInPictureParams; arm+disarm in effect; gate AppState fallback to <31; keep custom activity-level PiP (don't enable expo-video's own).
- [Multipart upload assembly — transactional redesign](multipart-upload-transactional.md) — completeMultipartUpload fully rewritten: advisory lock + part-sequence + per-part non-empty check + stale-blob delete + bytea_agg assembly + size validation + server-side SHA-256 — all in one db.transaction(); completedVideoId must never be cleared in watchdog/catch (orphans video from auto-recovery); pass session.expectedFileSha256 to completeMultipartUpload in both finalize and spawnAssemblyRetry.
- [Large-file assembly timeout — RETURNING fold + statement_timeout=0](large-file-assembly-timeout.md) — 5+ GB uploads timed out at ~126s: bytea_agg INSERT + separate octet_length SELECT + separate sha256 SELECT = 3 full blob reads. Fix: SET LOCAL statement_timeout=0 before assembly + fold size+sha256 into RETURNING on the INSERT (buffer-cache, not a second TOAST scan). 3 reads → 1.
- [EAS invalid-keystore-alias error = wrong key password](eas-pkcs12-keystore-alias-error.md) — PKCS12 keystores force keyPassword==storePassword; a mismatched keyPassword (e.g. "null") surfaces as misleading EAS_BUILD_INVALID_KEYSTORE_ALIAS_ERROR.
- [Admin videos total=-1 sentinel](admin-videos-count-sentinel.md) — list endpoint returns total=-1 in cursor mode; page-1 newest always hit it; run COUNT on page 1 + UI must never render a negative total.
- [s3_mirrored_at repair must key off object_path](s3-mirrored-repair-objectpath.md) — repairMissingS3MirroredAt + auditMissingBlobs must both prefer object_path over URL-derived key, else stamp-failed MP4s stay "not broadcast-ready" forever; repair takes optional videoId for targeted force-enqueue self-heal.
- [Inline blob-confirmation self-heal](inline-blob-confirmation-self-heal.md) — enqueueIfMissing self-heals a missing s3MirroredAt stamp synchronously in-call via isBlockedOnlyByMissingBlobConfirmation; eliminates the reconciler-tick/manual-click wait for "not yet broadcast-ready".
- [Validation failures surfaced as 500](zod-validation-500-crash.md) — validation-phase errors must map to 400 via validationContext/FST_ERR_VALIDATION (not instanceof ZodError); don't reject-empty a legitimately-empty querystring.
- [Notification dispatcher atomic claim pattern](notification-dispatcher-claim-pattern.md) — SKIP LOCKED claim + status-flip UPDATE must share one transaction; attempts counter must be separate from sentCount; stuck-recovery keys off claimedAt not scheduledAt.
- [DB pool saturation boot sequence fix](db-pool-boot-consolidation.md) — 9 sequential pool.connect() at startup collapsed to 2 (runPreBuildBootSequence + runPostBuildBootSequence), each using 1 connection; pool utilization dropped from 40/40 to ~10% at 30s.
- [db.execute() node-postgres rows shape](db-execute-rows-shape.md) — db.execute() on node-postgres returns { rows: T[] } not T[]; use firstRow<T>/allRows<T> helpers from infrastructure/storage.ts; raw [0] indexing silently returns undefined.
- [PG BYTEA 1GiB assembly ceiling — RESOLVED](chunked-blob-storage.md) — old bytea_agg 1GiB ceiling removed; uploads now promote parts into storage_blob_chunks (row-per-chunk, no aggregation); see topic file before touching storage.ts, integrity monitor, or any storage_blobs DELETE.
- [Mobile↔API contract-mismatch bug class](mobile-api-contract-mismatch.md) — client sends a field (e.g. logout `everywhere`) the server route accepts but never wires into logic; MFA-gated endpoints need a dedicated client error type + UI second-step, not a generic error.
- [Viewer count single source of truth](viewer-count-single-source-of-truth.md) — gateway-level heartbeat auto-registration into viewerTrackingService replaced dual raw-socket + orphaned-Redis-service viewer counting; one writer to broadcastEngine.setViewerCount().
- [Player no-source error UI and nav-latch safety valve](player-no-source-and-nav-latch-safety.md) — explicit "unavailable" state replaces silent blank placeholder; navInFlightRef gets a 4s timeout escape hatch so a swallowed navigation can't strand Prev/Next forever.
- [Broadcast daemon architecture](broadcast-daemon-architecture.md) — daemon owns broadcast engine (port 9000); API proxies to it; never restart daemon during API deployments.
- [Broadcast deployment resilience](broadcast-deployment-resilience.md) — 3 gaps closed: restart-log table+repo+admin card; daemon liveness monitor (_started flag not pollTimer); resilient SSE proxy with 30 s retry window.
- [YouTube override restart reload-flash bug](yt-override-restart-reload-flash.md) — engine resumes same video with correct position but a NEW override.id after a restart; client must compare by video id, not override.id, or it reloads/flashes an already-playing iframe.
