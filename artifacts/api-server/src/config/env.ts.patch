--- a/artifacts/api-server/src/config/env.ts
+++ b/artifacts/api-server/src/config/env.ts
@@ -50,6 +50,9 @@ const schema = z.object({
   // Production must set an explicit allowlist; dev allows localhost.
   CORS_ORIGINS: z.string().default(''),
   CORS_ORIGINS_EXTRA: z.string().optional(),
+  
+  // Admin token IP allowlist (comma-separated)
+  ADMIN_API_TOKEN_IP_ALLOWLIST: z.string().optional(),
 
   // JWT secrets (access + refresh tokens)
   JWT_ACCESS_SECRET: z.string().min(32),
