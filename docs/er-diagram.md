# Entity Relationship Diagram

Generated by hand from `apps/api/app/db/models/*.py` — not auto-generated,
so if the models change, this file needs a matching edit. It's checked
against the actual model definitions (field names, nullability, FK
`ondelete` behavior) as of Milestone 7, not just described from memory.

Three domains, three files: `geography.py` (countries/states + the trust
layer), `knowledge.py` (the actual answerable content), `auth.py` (users/
RBAC/API keys/audit). All share one `Base` and one Alembic migration
history, but the module split mirrors this diagram's groupings.

```mermaid
erDiagram
    %% --- Geography & trust layer ---
    COUNTRY ||--o{ STATE : "has"
    COUNTRY ||--o{ AGENCY : "has"
    COUNTRY ||--o{ LAW : "has"
    STATE ||--o{ AGENCY : "optionally scopes"
    STATE ||--o{ OFFICE : "optionally scopes"
    SOURCE ||--o{ VERIFICATION : "cited by"

    COUNTRY {
        uuid id PK
        string iso_code UK "e.g. 'NG'"
        string name
    }
    STATE {
        uuid id PK
        uuid country_id FK
        string name
        string code
    }
    SOURCE {
        uuid id PK
        string title
        string url
        string publisher
        bool is_primary
        timestamp accessed_at
    }
    VERIFICATION {
        uuid id PK
        enum entity_type "agency|office|service|requirement|document|fee"
        uuid entity_id "polymorphic — no real FK, see note below"
        uuid source_id FK
        enum status "verified|unverified|outdated|disputed"
        numeric confidence_score "0.00-1.00"
        timestamp verified_at
        text notes
    }

    %% --- Knowledge domain ---
    AGENCY ||--o{ OFFICE : "has"
    AGENCY ||--o{ SERVICE : "provides"
    LAW ||--o{ SERVICE : "authorizes"
    SERVICE ||--o{ REQUIREMENT : "has"
    SERVICE ||--o{ FEE : "has"
    SERVICE ||--o{ SERVICE_DOCUMENT : "requires"
    DOCUMENT ||--o{ SERVICE_DOCUMENT : "required by"

    AGENCY {
        uuid id PK
        uuid country_id FK
        uuid state_id FK "nullable, SET NULL on state delete"
        string name
        string short_name
        string website
    }
    OFFICE {
        uuid id PK
        uuid agency_id FK
        uuid state_id FK "nullable, SET NULL"
        string name
        string address_line
        bool is_headquarters
    }
    LAW {
        uuid id PK
        uuid country_id FK
        string title
        int year_enacted
    }
    SERVICE {
        uuid id PK
        uuid agency_id FK
        uuid law_id FK "nullable, SET NULL"
        string name
        string slug UK
        string category
        text description
        string typical_processing_time "nullable — free text, see model docstring"
        int version
        tsvector search_vector "GENERATED ALWAYS AS ... STORED"
    }
    REQUIREMENT {
        uuid id PK
        uuid service_id FK "CASCADE"
        string name
        bool is_mandatory
        int sort_order
    }
    DOCUMENT {
        uuid id PK
        string name UK
        text description
    }
    SERVICE_DOCUMENT {
        uuid service_id FK "CASCADE, composite PK"
        uuid document_id FK "CASCADE, composite PK"
        bool is_mandatory
        text notes
    }
    FEE {
        uuid id PK
        uuid service_id FK "CASCADE"
        string name
        numeric amount
        string currency "ISO 4217"
        bool is_mandatory
    }

    %% --- Auth domain ---
    USER ||--o{ USER_ROLE : "has"
    ROLE ||--o{ USER_ROLE : "granted to"
    ROLE ||--o{ ROLE_PERMISSION : "has"
    PERMISSION ||--o{ ROLE_PERMISSION : "granted via"
    USER ||--o{ API_KEY : "owns"
    USER ||--o{ AUDIT_LOG : "attributed to (nullable)"

    USER {
        uuid id PK
        string email UK
        string hashed_password "bcrypt"
        bool is_active
        bool is_superuser
    }
    ROLE {
        uuid id PK
        string name UK "e.g. 'member', 'editor'"
    }
    PERMISSION {
        uuid id PK
        string code UK "e.g. 'knowledge:write'"
    }
    USER_ROLE {
        uuid user_id FK "CASCADE, composite PK"
        uuid role_id FK "CASCADE, composite PK"
    }
    ROLE_PERMISSION {
        uuid role_id FK "CASCADE, composite PK"
        uuid permission_id FK "CASCADE, composite PK"
    }
    API_KEY {
        uuid id PK
        uuid user_id FK "CASCADE"
        string name
        string key_prefix "first 12 chars, shown in listings"
        string hashed_key UK "SHA-256, never the plaintext"
        timestamp revoked_at "nullable"
        timestamp expires_at "nullable"
    }
    AUDIT_LOG {
        uuid id PK
        uuid user_id FK "SET NULL — a failed login for an unknown email has no user"
        string action "e.g. 'user.login', 'service.created'"
        string entity_type
        uuid entity_id
        string ip_address
        jsonb audit_metadata
    }
```

## Notes on deliberate deviations from a "textbook" schema

**`Verification` is polymorphic, not six separate join tables.** `entity_type`
+ `entity_id` identify the fact being verified (a `Service`, `Fee`,
`Requirement`, `Document`, `Agency`, or `Office`), rather than
`service_verifications`, `fee_verifications`, etc. each duplicating
identical columns and query logic. The real cost: the database itself
cannot enforce that `entity_id` points at a row that actually exists in
whichever table `entity_type` names — there's no single table a foreign
key could reference. Repositories are responsible for only ever writing
valid `(entity_type, entity_id)` pairs (see
`app/repositories/knowledge_repository.py`'s module docstring).

**`Document` is shared across services, not owned by one.** The same
document type ("a valid means of ID") is required by many services;
`ServiceDocument` (an association *object*, not a bare many-to-many table)
carries the per-service `is_mandatory`/`notes` that differ by context.

**`Fee` is its own entity**, not folded into `Requirement` — an explicit
decision confirmed with the project owner before Milestone 2, since fees
and process-steps are different kinds of fact even though both attach to
a `Service`.

**No `Country`/`Agency`/`User` delete endpoints exist yet** — but every
one-to-many relationship that *would* be affected by one already has
`passive_deletes=True` set (see `knowledge.py`/`geography.py`/`auth.py`),
deferring cascade behavior to the database's `ON DELETE` rules rather than
SQLAlchemy's default (and, for `NOT NULL` child columns, broken) attempt
to null out foreign keys in Python first. Fixed proactively in Milestone 4
after the identical bug surfaced in `Service`'s child relationships — see
that milestone's commit for the full explanation.
